# 実装計画

**何を・どの順番で作るか。**進捗はこのファイルの「状態」列を更新して共有する。

- **設計は完了している。**何を作るかは [完全設計書](spec/README.md)（正本）と
  [詳細設計書](detailed/README.md) にある。ここはその**順番と進み具合**だけを扱う。
- 全体は **9 段階・49 の作業単位**（2026-09-20 に `WP-DEV-01` を `WP-UI-08` から切り出した）。**各段階の終わりに必ず「動くもの」が残る**ように切ってある。
- **段階は「作業のまとまり」であって実施順ではない**（2026-09-20 改定）。
  ~~段階をまたぐ順番は入れ替えられない~~ という旧原則は、特例デモで段階 4・7 を飛ばして
  5・6 に到達した時点で破れている。**実施順は用途で決める**（§6）。

---

## 1. 今どこにいるか

**状態は 2026-09-20 に実際のコードと突き合わせて更新した。**それ以前の表は
2026-08-25 時点のまま「実装 未着手」と書かれており、事実と大きく食い違っていた。

| | 状態 |
| --- | --- |
| 完全設計書（何が・どう振る舞うべきか） | **完了**（`docs/plan/spec/` が正本） |
| 詳細設計書（どう実装するか） | **段階 0〜2 まで完了。**段階 3 以降は着手時に書く（段階 3・6 は一部書いてある） |
| 実装 | **段階 0〜3 と段階 6 はほぼ到達。段階 4（追従）と段階 7（点検・校正）が空いたまま** |

**2026-09-01〜09-20 は特例運用でデモを優先し、段階の順番を飛ばして段階 5・6 に到達した**
（[EXCEPTION-LEDGER.md](EXCEPTION-LEDGER.md)）。**そのため「前の段階が安全装置を用意してから
次に進む」という §2 の原則は既に破れている。**飛ばしたぶんの借金は台帳 16 件と、
この表の「デモ経路で到達」行にある。

### 状態の書き方（証拠の水準）

**「完了」と書かない。**何を根拠にそう言えるかを列に出す。

| 表記 | 意味 | 判定に使った証拠 |
| --- | --- | --- |
| **実機確認済み** | 通電した実機で動作を見た | [実機作業キュー](../実機作業キュー.md) ／ `docs/試験場内動作試験.md` ／ `docs/教示再生走行試験.md` の該当行 |
| **配線済み** | `bringup.launch.py` が起動し、自動試験がある | `executable=` の一覧 ＋ `th_testing/test/` |
| **実装あり・未配線** | ファイルはあるが launch にいない | 同上の差分 |
| **デモ経路で到達** | 機能は動くが、その作業パケットとしては実施していない | 台帳 ID を併記 |
| **未着手** | — | — |
| **未検証** | 2026-09-20 の突き合わせで判定できなかった | — |

**`WP-` 番号は変えない。**[DetailedDesign-packets.md](detailed/DetailedDesign-packets.md) の
見出しと 1 対 1 で対応しているため。変えたのは状態列だけ。

---

## 2. 進め方の原則

**2026-09-20 改定。**旧版の「実装は Sonnet 5-high が行う」「段階をまたぐ順番は入れ替えられない」
「人が関与するのは管理者権限と通電実機だけ」は、いずれも実際の運用と合わなくなったので差し替えた。

| # | 原則 | 補足 |
| --- | --- | --- |
| 1 | **実装は herdr の `ImplementAgent` タブに常駐させた opencode に投げる** | 体数は固定しない。必要なだけペインを分けて増やす（下記 §2.1） |
| 2 | **順番は「段階」ではなく「用途」で決める** | 実際の運用で先に要るものから埋める（§6）。~~前の段階が安全装置を用意してから次へ~~ という旧原則は、デモで段階 4・7 を飛ばして 5・6 に到達した時点で破れている |
| 3 | **検証は必ず実装管理担当（人＋Claude）が自分で行う** | **実装エージェントの「テストが緑」報告は信用しない。**下記 §2.2 |
| 4 | **実機はモータの電源だけ切って PC に繋ぐ**（ESP32 はラズパイに USB 接続） | 疎通確認・モード遷移・UI 操作・フォルト検知の時間測定まで**通電なしで**できる |
| 5 | **完成の判定は実行できるコマンドで行う** | 「できたと思う」で先に進まない |

**通電（モータを回す）が要るのは走行を伴う確認だけ**——制動距離の測定、物理非常停止の動作確認、直進・旋回の校正。

### 2.1 実装エージェントの動かし方（herdr ＋ opencode）

**1 作業パケット ＝ 1 ブリーフ ＝ 1 エージェント**。並行数は作業の独立性で決める。

```bash
herdr tab list                                    # ImplementAgent タブの tab_id を確認
herdr pane split --pane <既存pane> --direction right   # 体を増やすときはペインを分ける
herdr agent list                                  # pane が agent="opencode" として自動検出されたか確認
herdr agent rename <pane_id> <name>               # 名前を付ける（start ではなく rename）
herdr agent prompt <name> "指示書は .briefs/brief-<ID>.md。全文を読み前提誤りを指摘、無ければ最後まで実装"
herdr agent get <name>                            # agent_status を 10 秒間隔でポーリングして監視
```

| 項目 | 決まりごと | 理由 |
| --- | --- | --- |
| ブリーフの置き場 | **`.briefs/`（リポジトリ内・gitignore 済み）** | リポジトリ外に置くと opencode が「外部ディレクトリアクセス許可」で毎ターン止まる |
| 一時ファイル | **`.briefs/tmp/`。`/tmp` を使わせない** | 同上。「Allow always」を選んでもターンを跨いで再発する |
| 既に opencode が動いているペイン | **`agent start` を打たない。`agent rename` だけ**で登録する | `start` はシェルプロンプト待ちを期待するため、動作中のセッションに文字列を打ち込んで壊す |
| 並行させるとき | **片方を `git worktree` に隔離する** | 同じファイルを 2 体が触ると衝突する。`herdr worktree` が helper を持つ |
| 監視 | `herdr agent get` のポーリング | `herdr agent wait --until` は状態の遷移を取りこぼす |
| 承認プロンプト | `herdr agent send-keys <name> Left Left Right Enter` で「Allow always」 | カーソル位置が読めないので左端へ寄せてから 1 つ右 |
| コミット | **細かく切らせる** | **無料枠・クォータで途中で止まる前提。**opencode は上限に達すると idle のまま進まなくなる |
| `git push` | **投げない。実装管理担当が検証してから押す** | — |

### 2.2 受け入れ検査（実装管理担当がやること）

**実装は信用してよい。テストは絶対に信用しない。**（実際に、通るだけで何も検出しないテストが繰り返し出てきた）

1. ホスト `pytest` / `npm run test:unit` / `npx playwright test` を**自分で回す**
2. **変異チェックを 1〜2 件は自分で再実行する** — 実装ファイルを `sed` で壊し、該当テストが赤くなるか
3. `git show <commit>` で差分がブリーフの範囲内かを目視する
4. ROS2 が要るものは Docker で `colcon build` ＋ 起動スモークまで自分で
   （ホストだけで走る試験の範囲は `CLAUDE.md`「環境の癖」を見る）

**機器が揃っていなくても検証できる。**`dev_mode:=true` で起動すれば、実機・ラズパイ・
ESP32 のどれかが無くても `IDLE` まで進んで画面と FSM を確かめられる
（使い方は `CLAUDE.md`「開発モードを使う」）。**ただし開発モードで通した結果を
通常モードの結果と取り違えないこと。**

### 2.3 git の運用（実装管理担当が持つ）

**ブランチ・マージ・push・掃除はすべて実装管理担当の責任**（2026-09-20 ユーザー決定）。
実装エージェントは**自分の作業ブランチにコミットするところまで**で、それ以外の git 操作はしない。

| 操作 | 誰が | 備考 |
| --- | --- | --- |
| ブランチを切る | **実装管理担当** | ブリーフを渡す前に切り、エージェントにはブランチ名を伝える |
| コミットする | 実装エージェント | **細かく切らせる**（クォータで途中停止する前提。§2.1） |
| `main` へマージ | **実装管理担当** | §2.2 の受け入れ検査を通してから |
| `git push` | **実装管理担当** | エージェントには投げさせない |
| ブランチの掃除 | **実装管理担当** | 下記「掃除」 |

#### ブランチ

- 命名は **`<種別>/<主題>`**。種別は実績に合わせて `feat` / `fix` / `docs` / `tune` / `chore` / `test`。
  作業パケット単位なら記号を入れる（例: `feat/wp-safe-05-localization-lost`）。
- **1 作業パケット ＝ 1 ブランチ。**複数のパケットを 1 本に混ぜない
  （混ぜると片方だけ差し戻せない。`git add -p` が使えない環境なので後から分割もできない）。
- **文書だけの修正は `main` へ直接コミットしてよい。**コードを含むものは必ずブランチを切る。
- **並行させるときは片方を `git worktree` に隔離する**（§2.1）。同じファイルを 2 体が触ると衝突する。

#### マージ

- **`--no-ff` でマージする。**作業の単位が履歴に残る。
- **squash しない。**`EXCEPTION-LEDGER.md` や設計文書がコミットハッシュを名指しで参照しているため、
  潰すと追跡が切れる。
- マージコミットの本文に**検証の結果を数字で書く**（例: ホスト pytest 910 passed、web_ui 208 e2e passed）。
  後から「何を根拠にマージしたか」を追えるようにする。

#### コミットメッセージ

`<種別>(<範囲>): <日本語の 1 行>` ＋ 本文に**何をなぜ**（どう直したかは差分を見れば分かる）。
末尾の帰属行は会話の指示に従う。

#### 掃除

- **マージしたブランチはその場で消す**（ローカル・`origin` とも）。溜めない。
- **放置されたブランチは四半期ごとに棚卸しする。**`main` が大きく動いたあとは、古いブランチの
  土台が古すぎて rebase より作り直しが速いことが多い。
- **2026-09-20 に棚卸しして 37 本を消した**（`main` が 412 コミット動き、全部が古い土台だった）。
  内訳は未マージ 15 本 ＋ `main` へ取り込み済み 22 本。**消したブランチの SHA は
  コミット `BRANCH-CLEANUP` のメッセージに残してある**（`git log --grep='ブランチを棚卸し'`）。
  必要なら `git branch <name> <sha>` で復元できる。

#### 長期保管するブランチ（`main` には統合しない）

**本来の機能ではないので `main` に入れない。展示などで必要になったときに使う**
（ユーザー決定 2026-09-20）。**掃除の対象外。**

| ブランチ | 中身 |
| --- | --- |
| `tune/exhibition-demo` | 展示ブース用の `FACING` モード（人の方を向く）＋ 旋回速度上限の WebUI 調整 ＋ 音声 |
| `feat/zundamon-voice` | 展示専用の話者切替（ずんだもん） |

#### `CLAUDE.md` と重複させない

作業前の `git status` 確認、自分が変更したファイルだけをステージする、`git add -A` を使わない、
`git add -p` が使えないので 1 ファイルに複数の関心事を混ぜない ── これらは
`CLAUDE.md`「作業開始前のルール」が正。ここには書かない。

---

## 3. 全体像 —— 9 段階

**「終わりに何ができるようになるか」で読む。**

> **「段階」と `stage:=N` は別物。**ここでいう段階 0〜8 は作業の順番であり、
> `bringup.launch.py` の起動引数 `stage:=1`〜`4`（どのノード群まで起動するか）とは
> 一致しない。例: 試験場内動作（段階 6）は `stage:=4` で起動する。

| 段階 | やること | 終わったら何ができるか | 状態（2026-09-20） |
| --- | --- | --- | --- |
| **0** | **土台づくり** | 実機の性能を測り終え、共通の型と設定の置き場ができる | **ほぼ完了。**測定 3 件（`WP-MEAS-02/03/05`）が残る |
| **1** | **骨格** | 実機で電源を入れて待機状態にでき、**非常停止がいつでも押せる** | **完了**（実機確認済み） |
| **2** | **安全装置** | **壊しても止まる。**13 通りの故障を自動で試験できる | **配線済み。**ただし `DRIVE_RUNAWAY` は台帳 `W-06` で**無効のまま**。故障注入は実機専用 3 本が未実施 |
| **3** | **手動で走らせる** | タブレットのスティックで走る。走った道を記録できる | **到達**（実機確認済み）。記録・教示はデモ経路（`W-03`/`W-04`） |
| **4** | **人について行く** | 試験員を選んで追従する | **空いている。**人物トラッカーと対象選択は段階 6 のために作ったが、**追従走行（`FOLLOW`）そのものは新設計で未実装** |
| **5** | **覚えた道を走る** | 記録した経路を再生する（逆走も） | **順再生はデモ経路で到達**（実機確認済み）。**逆再生は画面が無効で実機も未確認**（台帳 `W-19`）。**全域ローカライズ（`W-01`）と自己位置ロスト検知（`WP-SAFE-05`）が無い** |
| **6** | **試験場での 1 日** | 前日に地図と目印を作り、当日は配電盤の間を行き来する | **到達**（2026-09-14 実機で全盤巡回成功）。方式 B・地図修正・書き足しは台帳 `W-08`/`W-10`/`W-11` |
| **7** | **点検と校正** | **1 日の作業がすべてタブレットで完結する**（最終形） | **空いている。**`OPCHECK`/`CALIB` は**モードだけ在って runner が無く、入ると戻れない**（下記） |
| **8** | **ライン誘導・電子リード** | 後回し。カメラの選定だけ先に行う | 未検証 |

> **段階 3 で「手動走行」を作る理由**: 他の走り方が使えないときの**避難先**になるため。ここが動けば、以降どの段階で失敗しても運用は続けられる。

### いま分かっている危険

| # | 内容 | 場所 |
| --- | --- | --- |
| **1** | **`DRIVE_RUNAWAY`（駆動系の暴走検知）が無効のまま `main` に載っている** | 台帳 `W-06`。除外理由だった「WiFi 経由の受信ギャップ」は ESP32 のシリアル化（2026-09-05）で消えている可能性が高い |
| ~~**2**~~ | ~~`OPCHECK` / `CALIB` に入ると戻れない（詰み）~~ **2026-09-20 に確認したところ既に解消済み。** `mainMenuItems.needsFinishEscape()` ＋ S-01 の脱出ボタン（`ui.finish`）が実装され、試験も在る（unit 9 件 ＋ e2e 3 件。`OPCHECK` で実際にボタンが出て `ui.finish` が飛ぶことを確認） | 残る別件: **駆動するノード（runner）が `bringup.launch.py` にいない**・`th_calibration/scripts/` の 4 本も launch 配線なし（`WP-MAINT-01`/`-02` の本体作業） |
| **3** | **自己位置を見失っても止まらない。**`LOCALIZATION_LOST` は型（`FaultStatus.msg`・`safety_monitor_core.cpp` の文字列）だけ在り、`SAFETY_ENABLED_TARGETS` に `localization` が無い | 台帳 `W-02` ／ `WP-SAFE-05` |
| **4** | 旧設計の幽霊ノード 3 本（`follow_planner` / `follow_planner_mapless` / `person_predictor`）が残存。**廃止済みトピック `/cmd_vel_retreat` へ publish している** | 実機では起動しない（`use_stub:=true` のときだけ）ので実害は無いが、`WP-CLEAN-01` の対象 |

---

## 4. 段階ごとの作業

**「作業単位」の記号（`WP-…`）は詳細設計書の見出しと 1 対 1 で対応する。**

> **状態は 2026-09-20 に実際のコードと突き合わせた。**判定の根拠は §1「状態の書き方」。
> 実機が要る作業は [実機作業キュー](../実機作業キュー.md) に集約してある。

### 段階 0 — 土台づくり（11 件・[詳細](detailed/DetailedDesign-wp0.md)）

| # | 作業 | 記号 | 状態 |
| --- | --- | --- | --- |
| 1 | 通信で使うデータの型を決める | `WP-MSG-01` | **配線済み** |
| 2 | **状態遷移表**とその実行部品 | `WP-STATE-01` | **配線済み**（`th_state/config/*.yaml` ＋ `state_core`） |
| 3 | **設定値の台帳**（数値は全部ここにだけ書く） | `WP-PARAM-01` | **配線済み。**ただしデモで作ったノード群は registry 経由でない（台帳 `W-03`/`W-13`） |
| 4 | 通信の途切れ具合を測る仕組み | `WP-SAFE-00` | **配線済み** |
| 5 | Wi-Fi の改善・パスワード混入の是正 | `WP-NET-01` | **前提が変わった。**ESP32 は無線を使わない（[Spec.md](spec/Spec.md) §6.1）。`wifi_credentials.h` は .gitignore 済み |
| 6 | **物理非常停止を効くようにする** | `WP-ESP32-01` | **実機確認済み** |
| 7 | **止まるまでの距離を実測**（通電） | `WP-MEAS-01` | **実機確認済み**（`brake_accel_mps2` = 1.18・2026-08-21） |
| 8 | 人が歩いている部屋で地図を作れるか確認（通電） | `WP-MEAS-02` | **デモ経路で部分的に到達**（`PREP/MAPPING` で人がいる中の地図作成を実機で実施）。**KPI は未計測**。要否判断が台帳 `W-10` の入力 |
| 9 | 今の手押し作業の時間を測る（比較用） | `WP-MEAS-03` | 未着手 |
| 10 | 通信の途切れ具合を実測 | `WP-MEAS-04` | **実機確認済み**（esp32/lidar）。`ui_gap_p99_ms` は未測定（`N-5`） |
| 11 | 1 日通しの完走試験 ※段階 6 の後 | `WP-MEAS-05` | 未着手（2026-09-14 に実機通しは成功したが KPI 計測ではない） |

**この段階の終わり**: 測った値が台帳に入り、単体試験が通る。

### 段階 1 — 骨格（6 件・[詳細](detailed/DetailedDesign-wp1.md)）

| # | 作業 | 記号 | 状態 |
| --- | --- | --- | --- |
| 1 | 状態を管理する本体 | `WP-STATE-02` | **実機確認済み** |
| 2 | 起動時の疎通確認 | `WP-STATE-03` | **実機確認済み** |
| 3 | 設定値の検査と読み込み | `WP-PARAM-02` | **配線済み**（`params_audit`。台帳 `W-15` で一部の起動停止を緩和中） |
| 4 | 画面の共通部分（ヘッダ・ボタン・警告窓） | `WP-UI-01` | **実機確認済み**（`shell/AppShell` ＋ W-1〜W-6） |
| 5 | 接続確認画面とメインメニュー | `WP-UI-02` | **実機確認済み**（S-00 / S-01） |
| 6 | 手押しモード | `WP-CARRY-01` | **実機確認済み**（2026-08-31） |

**この段階の終わり**: 実機（電源断）で待機状態まで進み、**どの画面からでも非常停止が押せる**。

### 段階 2 — 安全装置（6 件・**最重要**・[詳細](detailed/DetailedDesign-wp2.md)）

| # | 作業 | 記号 | 状態 |
| --- | --- | --- | --- |
| 1 | 指令が途切れたら車輪を止める | `WP-SAFE-02` | **実機確認済み**（ESP32 ウォッチドッグ 600 ms ＋ twist_mux） |
| 2 | **LiDAR の死角の校正** | `WP-CALIB-01` | **実機確認済み**（4 セクタ実測。`a70a2cc` / `f1cbc26`） |
| 3 | 異常の重さを 2 段階に分ける | `WP-SAFE-01` | **実機確認済み。**保持時間とフォルト起因 ESTOP からの復帰を実機フィードバックで追加（[Spec-safety.md](spec/Spec-safety.md) §3.5.1・§3.5.2） |
| 4 | **障害物リミッタ** | `WP-SAFE-03` | **実機確認済み。**単一点で止めない改修済み（同 §2.5）。`v_reverse`/`v_slow`/`v_jog_panel` の既定値は台帳 `W-05`/`W-18` |
| 5 | 動いてはいけない場面で手動操作を無効にする | `WP-SAFE-04` | **配線済み**（`jog_gate`）。`jog_gate.yaml` に死パラメータが残る（`W-07`） |
| 6 | **13 通りの故障を意図的に起こす自動試験** | `WP-TEST-01` | **配線済み**（14 本）。**実機専用 3 本（04/08/10）が未実施**。`DRIVE_RUNAWAY` は `W-06` で無効のため試験対象外 |

**この段階の終わり**: 意図的に壊しても止まることを、**人が見ていなくても**確認できる。

### 段階 3 — 手動で走らせる（4 件）

| 作業 | 記号 | 状態 |
| --- | --- | --- |
| 走行操作パネル（スティック・速度プリセット） | `WP-UI-03` | **実機確認済み。**その場旋回をプリセットから切り離す改修済み（[Spec-transit.md](spec/Spec-transit.md) §2.2）。比率×上限の設計欠落は `W-07` |
| 手動走行 | `WP-TRANSIT-01` | **実機確認済み**（S-11） |
| 走った道の記録と保存 | `WP-ROUTE-01` | **デモ経路で到達**（`route_recorder`。数値が registry 経由でない＝`W-03`） |
| 教示（手動で走って道を覚えさせる） | `WP-TRANSIT-02` | **デモ経路で到達**（S-13 / `TEACH_MANUAL`）。教示（追従）は段階 4 |

### 段階 4 — 人について行く（5 件）

**ここが一番空いている。**段階 6 のために人物トラッカーと対象選択は作ったが、
**追従走行そのもの（`FOLLOW` モード）は新設計では一度も実装していない。**

| 作業 | 記号 | 状態 |
| --- | --- | --- |
| 人の検出と候補の一覧化 | `WP-PERC-01` | **部分到達**（`person_tracker_bridge` ＋ 対象選択レーダーを段階 6 で実機使用） |
| 追従走行 | `WP-TRANSIT-03` | **未着手。**`th_transit` パッケージは存在しない。`follow_planner.py` は旧設計で廃止対象 |
| 追従画面（候補レーダー・対象選択） | `WP-UI-04` | **部分到達**（対象選択の部品は S-20/S-21 側にある。S-10 は無い） |
| 教示（追従しながら道を覚えさせる） | `WP-TRANSIT-04` | 未着手 |
| 「追跡を切った」と「追跡が壊れた」の区別 | `WP-STATE-04` | **部分到達**（`require_explicit_target_selection` ＝ 自動復帰しない設計） |

### 段階 5 — 覚えた道を走る（4 件）

| 作業 | 記号 | 状態 |
| --- | --- | --- |
| 地図の管理（試験場用と経路用を分ける） | `WP-ROUTE-02` | **デモ経路で到達**（`map_session`。別ストア） |
| 自己位置を見失ったら止まる | `WP-SAFE-05` | **未着手。**`LOCALIZATION_LOST` は型だけ在り、`SAFETY_ENABLED_TARGETS` に `localization` が無い（台帳 `W-02`） |
| 経路の再生（順走・逆走） | `WP-ROUTE-03` | **順再生はデモ経路で到達**（実機確認済み）。**逆再生は実装ありだが実機未確認**（2026-09-04 に 1 回試して 1.4 m で PAUSE。地図読み直しバグは WS-9S で修正済みだが再確認していない。台帳 `W-19`）。**全域ローカライズが無く、毎回ほぼ同じ物理位置から始める必要がある**（台帳 `W-01`） |
| 再生画面 | `WP-UI-05` | **デモ経路で到達**（S-14。再生速度あり）。**「逆再生」ボタンは `disabled` のまま**で `reverse` は常に false（台帳 `W-19`） |

### 段階 6 — 試験場での 1 日（6 件）

| 作業 | 記号 | 状態 |
| --- | --- | --- |
| 2 点を指して場所と向きを登録する | `WP-ONSITE-01` | **実機確認済み**（方式 A のみ。方式 B は台帳 `W-08`。方式 C = 地図タップを 2026-09-14 に追加） |
| 配電盤・待機場所への移動 | `WP-ONSITE-02` | **実機確認済み**（2026-09-14 に panel1〜5 全巡回 ＋ 待機場所復帰） |
| 呼び寄せ時に人が退くのを待つ | `WP-ONSITE-03` | **配線済み**（`wait_clear_gate`）。**実機での退避ゲートは未計測**（台帳 `W-14`） |
| 地図の修正（人の映り込みを消す） | `WP-ONSITE-04` | 未着手（台帳 `W-10`） |
| 試験準備の画面 | `WP-UI-06` | **実機確認済み**（S-20） |
| 試験当日の画面 | `WP-UI-07` | **実機確認済み**（S-21）。走行中の非常停止・中断導線は未検証 |

### 段階 7 — 点検と校正（7 件）

**ここも空いている。しかも「モードだけ在る」状態が危険。**

| 作業 | 記号 | 状態 |
| --- | --- | --- |
| 始業点検 4 項目 | `WP-MAINT-01` | **未着手。**`OPCHECK` モードは FSM に在るが runner が無く、**入ると S-01 へ戻れない** |
| 校正（直進・旋回・IMU）と履歴 | `WP-MAINT-02` | **実装あり・未配線。**`th_calibration/scripts/` に 4 本あるが `bringup.launch.py` にいない（起動方法はログ文言に書いてあるだけ）。`CALIB` モードも runner 無し |
| 校正結果を機体へ反映する | `WP-ESP32-02` | **部分到達**（`calib.yaml` があれば bringup が ESP32 パラメータに重ねる。生成は `apply_calib.py` を手で叩く） |
| 故障診断 | `WP-MAINT-03` | 未着手 |
| 点検・校正・設定の画面 | `WP-UI-08` | **部分到達**（S-50 設定のみ実装。点検・校正の画面は無い） |
| **開発モードの実体** | **`WP-DEV-01`** | **土台は配線済み**（2026-09-20・`766cc59`）。`dev_mode:=true` で機器ゼロから `IDLE` に到達できることを Docker 実走で確認。**残るのは WebUI 接続・選択ログ記録・battery/opcheck/auto_brake の実ゲート**（§6 第 0 群） |
| **古いプログラムの削除** | `WP-CLEAN-01` | 未着手（幽霊ノード 3 本 ＋ `panel_navigator` が残存） |

### 段階 8 — 後回し

カメラの選定（`WP-LINE-00`）だけ**先に行う**（発注に時間がかかるため）。**未検証。**
ライン誘導と電子リードの実装は未着手。

---

## 5. 各段階の確認方法

| 段階 | シミュレーション | 実機 |
| --- | --- | --- |
| 0 | — | **測定 4 件**（通電） |
| 1 | 起動して待機まで | 電源断で待機状態まで |
| 2 | **故障注入 13 項目** | 電源断で停止確認／**通電で物理非常停止** |
| 3 | 狭い部屋 | 通電で手動走行 |
| 4 | 散らかった部屋／見失う場面 | 通電で追従 |
| 5 | 広い場所 | 通電で教示 → 再生 |
| 6 | 配電盤の往復 | 通電で前日準備 ＋ 当日運用 |
| 7 | — | 通電で点検・校正 |

> **この表は「段階ごとに何を確かめるか」であって、実施順ではない**（§6 が実施順）。

**自動試験は全段階で共通**（部品ごとの単体試験 → 結合試験。段階 2 以降はランダム入力試験を追加）。

```bash
bash scripts/run_tests.sh              # 単体試験のみ（最速）
bash scripts/run_tests.sh --all --sim  # 単体 ＋ 結合 ＋ シミュレーション
```

---

## 6. 次にやること

**用途順（2026-09-20 ユーザー決定）。**段階番号の順ではなく、**実際の運用で先に要るもの・
安全にかかわる穴から**埋める。追従走行は最後。

### 第 0 群 — 開発モード（**最優先**。2026-09-20 ユーザー決定）

| # | 作業 | 記号 | 理由 |
| --- | --- | --- | --- |
| ~~0-A~~ | ~~**開発モードの土台**（ROS 側の正本・`dev_mode` 引数・機器未接続の無視）~~ | `WP-DEV-01` | **完了**（2026-09-20・`766cc59`）。`dev_mode:=true` で**機器ゼロから `IDLE` に到達できる**ことを Docker 実走で確認した。`dev_mode:=false` は従来どおり `INIT/CHECK` で止まる |
| **0-B** | **WebUI を `/system/dev_mode` に繋ぐ** | `WP-DEV-01` | いま S-50 のトグルは `localStorage` だけで ROS 側に届かない。**画面から開発モードを切り替えられるようにする**（正本は ROS 側） |
| **0-C** | **選択ログ記録** | `WP-DEV-01` | [Spec-webui.md](spec/Spec-webui.md) §5「選択した特定のログのみをタイムスタンプ付きで記録」 |
| **0-D** | battery / opcheck / auto_brake の実ゲート | `WP-DEV-01` | **as-built に「運用開始を止めるゲート」がまだ無い。**選択状態の保持・配信だけ先に入れてある。ゲートを作る段（`WP-MAINT-01` 等）で繋ぐ |

**使い方**: `ros2 launch th_bringup bringup.launch.py dev_mode:=true`。
実行中の切替は `ros2 param set /connectivity_checker dev_mode true|false`。
現在の状態は `/system/dev_mode`（JSON）で読める。

**土台で分かったこと**: 物理 E-Stop の状態が**まだ分からない間**だけ、開発モードは運用開始を通す
（ESP32 が居ないと状態が一度も届かないため）。**押されていると分かっている間は開発モードでも止まる。**
[Spec-webui.md](spec/Spec-webui.md) §5.1 に表で明記した。

**現状はトグルとヘッダ表示だけで中身が無い。**`localStorage['th.devMode']` ＋ S-50 の開発モードタブ ＋
ヘッダのバッジはあるが、**警告を無視する処理もログの選択記録も実装されていない**
（`WP-UI-08` から切り出して `WP-DEV-01` にした）。正本は
[Spec-webui.md](spec/Spec-webui.md) §5 ／ [Spec-safety.md](spec/Spec-safety.md) §10。

**安全の境界は動かさない。**物理非常停止・ESP32 ウォッチドッグ・UI 非常停止・自律系の障害物停止は
開発モードでも無効化できない。`safety_monitor` と `obstacle_limiter` に `dev_mode` を**渡さない**
（[DetailedDesign-names.md](detailed/DetailedDesign-names.md) §1.3）ことで構造的に保証する。
**`dev_mode` は launch 引数としての設計があるだけで、まだ実装されていない**（`bringup.launch.py` に
宣言が無く `registry.yaml` にも無い）。このパケットで作る。

**検証する側（AI エージェントを含む）が WebUI を開かずに使えるようにする**（ユーザー決定 2026-09-20。
[Spec-webui.md](spec/Spec-webui.md) §5.1）。検証のたびに安全装置の解除で手間取っていたため、
起動引数と実行中のパラメータの両方から有効にできるようにする。
**`SD-1`（操作系は WebUI のみ）に対する意図的な例外**で、走行の操作を CLI に開くものではない。

**`O-d6`（権限・誤有効化の対策）は据え置き。**開発段階では制限をかけず、`?dev=1` と
S-50 のトグルもそのまま。**通常運用に入る直前に対策を決める**（同決定）。
`WP-DEV-01` を閉じるときに `O-d6` を消さないこと。

### 第 1 群 — 安全の穴（開発モードの次）

| # | 作業 | 記号・台帳 | 理由 |
| --- | --- | --- | --- |
| **1** | **`DRIVE_RUNAWAY` を戻す** | `W-06` | **駆動系の暴走検知が無効のまま `main` に載っている。**除外理由だった「WiFi 経由の受信ギャップ」は、**2026-09-21 の実機計測（25 分・欠落ゼロ・500 ms 超 0 件）で消えたことを確認した**（`docs/実機作業キュー.md` §4.0）。**ただし「戻すだけ」では済まない**: 最大ギャップ 480 ms が `runaway_hold_ms`（500 ms）に 20 ms 差まで迫るため、**先に台帳 `W-06` の②（wheel_feedback の鮮度ゲート）を入れてから `runaway` を戻す**。`DRIVE_RUNAWAY` の検知そのものは**駆動電源のある走行日でないと確かめられない**（電源なしで確認できたのは前提の消滅まで） |
| **2** | **自己位置を見失ったら止まる** | `WP-SAFE-05` ／ `W-02` | 型だけ在って検知が無い。**地図上の経路をなぞる動作が、ずれたまま続く。**段階 5・6 を実運用する前提条件。**検知条件は 2026-09-20 に決定（A・B・C をすべて採用）**＝ ①補正が凍る ②マッチの確からしさが低い ③推定ノードが死んだ（[Spec-safety.md](spec/Spec-safety.md) §3.5.0）。**使っていないモードでは監視しない**こと。②の信号源だけ実機確認待ち（`O-e1`） |
| ~~**3**~~ | ~~`OPCHECK` / `CALIB` の詰みを塞ぐ~~ | — | **作業不要。2026-09-20 に実物を確認したところ既に塞がっていた。**S-01 に `needsFinishEscape()` による脱出ボタンがあり、`ui.finish` → `IDLE` で抜けられる。unit 9 件・e2e 3 件で縛られている（`OPCHECK` を含む）。**「詰む」という記録の方が古かった。** |

### 第 2 群 — 試験場内動作を仕上げる（いま実際に使っている機能）

| # | 作業 | 記号・台帳 | 理由 |
| --- | --- | --- | --- |
| 4 | 走行中の非常停止・中断導線を実機で確認する | `WP-UI-07` | 2026-09-14 の実機試験で**未検証**のまま残った |
| 5 | 退避待ちゲートの実機計測、場内シナリオの自動試験 | `W-14` ／ `WP-ONSITE-03` | 場内動作の回帰を自動で守れていない |
| 6 | 盤前到着の許容差を実機で確かめる | `Spec-onsite.md` §6.2 | 0.25 → 0.12 に詰めた。**スリップで収束しない可能性があり実機でしか分からない** |
| 7 | 数値を `registry.yaml` 駆動に戻す | `W-03` / `W-13` / `W-15` | 現場調整が WebUI からできず、値の出どころが残らない（`Spec.md` §5 SD-9 違反） |
| 8 | 会場で測る 3 値 ＋ `blind_clearance_m` の設計判断 | `W-05` / `W-18` / `O-a2` | 速度上限の根拠が実測でない。`blind_clearance_m` は**測定ではなく人が決める** |
| 8-a | **教示逆再生を有効にする** | `W-19` ／ `WP-ROUTE-03` / `WP-UI-05` | 裏側は実装済みだが S-14 のボタンが `disabled`。**実機で試したのは 2026-09-04 の 1 回だけ**（1.4 m で PAUSE。WS-9S 修正後は未確認）。**先に実機で逆再生が動くかを確かめ**、その結果でトグル有効化と終点マーク運用の要否を決める |
| 8-b | 再生の全域ローカライズ・経路途中復帰 | `W-01` | 毎回教示始点とほぼ同じ物理位置から始める必要がある。**規模が大きい**（保存地図データベース＋全域ローカライザ＋READY 確認画面）。始点マーク運用で回るあいだは 第1群 #2（`WP-SAFE-05`）より下 |

### 第 3 群 — 点検と校正（段階 7）

| # | 作業 | 記号 | 理由 |
| --- | --- | --- | --- |
| 9 | 校正を launch に配線する | `WP-MAINT-02` / `WP-ESP32-02` | **スクリプトは 4 本あるのに launch にいない。**いまは手で `ros2 run` する必要がある |
| 10 | 始業点検 4 項目 | `WP-MAINT-01` | 1 日の作業をタブレットで完結させるのに要る |
| 11 | 故障診断・点検/校正の画面 | `WP-MAINT-03` / `WP-UI-08` | 開発モードの中身は第 0 群で先に作る（`WP-DEV-01`） |

### 第 4 群 — 追従走行（最後）

| # | 作業 | 記号 | 理由 |
| --- | --- | --- | --- |
| 12 | 追従走行（`FOLLOW`）を新設計で実装する | `WP-TRANSIT-03` | **新設計では一度も実装していない。**人物トラッカーと対象選択は段階 6 のぶんが流用できる |
| 13 | 追従画面・教示（追従） | `WP-UI-04` / `WP-TRANSIT-04` | — |
| 14 | 旧設計の幽霊ノードを消す | `WP-CLEAN-01` | 12 を作る時点で `follow_planner` 系の去就が決まる |

### いつでもできる（実機不要）

- `WP-MEAS-03`（手押し作業の時間を測る。比較の基準線）
- `WP-LINE-00`（カメラの選定。**発注に時間がかかるので先に動かす**）

---

## 7. 用語

| 語 | 意味 |
| --- | --- |
| **作業単位（`WP-…`）** | 1 セッションで完結する作業のまとまり。49 個ある |
| **状態遷移表** | 「どのモードで・何を操作したら・何が起きるか」を並べた表。**表にない動きは起きない** |
| **故障注入** | わざと壊して、ちゃんと止まるかを確かめる試験 |
| **教示** | 人が一度走らせて、その道を機体に覚えさせること |
| **通電 / 電源断** | 通電＝モータを回せる状態。電源断＝モータの電源だけ切って PC と繋いだ状態 |
| **Gazebo** | ロボットのシミュレータ。実機を使わずに動作を確かめる |

---

## 8. 関連文書

| 文書 | 中身 |
| --- | --- |
| [`detailed/README.md`](detailed/README.md) | **詳細設計書。**各作業単位の実装内容と完了条件 |
| [`detailed/DetailedDesign-packets.md`](detailed/DetailedDesign-packets.md) | 49 作業単位の一覧・依存関係・順番の制約 |
| [`spec/README.md`](spec/README.md) | 完全設計書。何が・どう振る舞うべきか |
| `docs/plan/spec/` | 目指す完成形。**確定した方針はここが正** |
| [`detailed/DetailedDesign-open.md`](detailed/DetailedDesign-open.md) | 未解決事項とレビュー記録 |
