# 実装計画

**何を・どの順番で作るか。**進捗はこのファイルの「状態」列を更新して共有する。

- **設計は完了している。**何を作るかは [詳細設計書](detailed/README.md)（10,600 行）にある。ここはその**順番と進み具合**だけを扱う。
- 全体は **9 段階・48 の作業単位**。**各段階の終わりに必ず「動くもの」が残る**ように切ってある。
- 段階をまたぐ順番は入れ替えられない。**前の段階が安全装置を用意してから次に進む**ため。

---

## 1. 今どこにいるか

| | 状態 |
| --- | --- |
| 完全設計書（何が・どう振る舞うべきか） | **完了** |
| 詳細設計書（どう実装するか） | **段階 0〜2 まで完了。**段階 3 以降は着手時に書く |
| `VISION.md` の更新 | **完了**（`8c02081`）。§0 で完全設計書・詳細設計書へ委譲し、目的・揺るがない前提・両設計書が扱わない事項の 3 つだけを残した |
| 実装 | **未着手** |

**次にやること**は §6。

---

## 2. 進め方の原則

| # | 原則 | 補足 |
| --- | --- | --- |
| 1 | **実装は Sonnet 5-high が行う** | 1 つの作業単位＝1 セッションで完結する形に分けてある |
| 2 | **実装 → 単体試験 → Gazebo シミュレーション → 実機試験 → 次の段階** | 段階を飛ばさない |
| 3 | **人が関与するのは「管理者権限が要る操作」と「通電した実機での確認」だけ** | それ以外は自動 |
| 4 | **実機はモータの電源だけ切って PC に繋ぐ**（ESP32 も USB 接続） | 疎通確認・モード遷移・UI 操作・フォルト検知の時間測定まで**人の関与なしで**できる |
| 5 | **完成の判定は実行できるコマンドで行う** | 「できたと思う」で先に進まない |

**通電（モータを回す）が要るのは走行を伴う確認だけ**——制動距離の測定、物理非常停止の動作確認、直進・旋回の校正。

---

## 3. 全体像 —— 9 段階

**「終わりに何ができるようになるか」で読む。**

| 段階 | やること | 終わったら何ができるか | 状態 |
| --- | --- | --- | --- |
| **0** | **土台づくり** | 実機の性能を測り終え、共通の型と設定の置き場ができる | 未着手 |
| **1** | **骨格** | 実機で電源を入れて待機状態にでき、**非常停止がいつでも押せる** | 未着手 |
| **2** | **安全装置** | **壊しても止まる。**13 通りの故障を自動で試験できる | 未着手 |
| **3** | **手動で走らせる** | タブレットのスティックで走る。走った道を記録できる | 未着手 |
| **4** | **人について行く** | 試験員を選んで追従する | 未着手 |
| **5** | **覚えた道を走る** | 記録した経路を再生する（逆走も） | 未着手 |
| **6** | **試験場での 1 日** | 前日に地図と目印を作り、当日は配電盤の間を行き来する | 未着手 |
| **7** | **点検と校正** | **1 日の作業がすべてタブレットで完結する**（最終形） | 未着手 |
| **8** | **ライン誘導・電子リード** | 後回し。カメラの選定だけ先に行う | 未着手 |

> **段階 3 で「手動走行」を作る理由**: 他の走り方が使えないときの**避難先**になるため。ここが動けば、以降どの段階で失敗しても運用は続けられる。

---

## 4. 段階ごとの作業

**「作業単位」の記号（`WP-…`）は詳細設計書の見出しと 1 対 1 で対応する。**

### 段階 0 — 土台づくり（11 件・[詳細](detailed/DetailedDesign-wp0.md)）

**実施順に並んでいる。**測定は実機が要るので、機体が使える日にまとめる。

| # | 作業 | 記号 | 状態 |
| --- | --- | --- | --- |
| 1 | 通信で使うデータの型を決める | `WP-MSG-01` | 未着手 |
| 2 | **状態遷移表**（どの操作で何が起きるかの一覧）とその実行部品 | `WP-STATE-01` | 未着手 |
| 3 | **設定値の台帳**（数値は全部ここにだけ書く） | `WP-PARAM-01` | 未着手 |
| 4 | 通信の途切れ具合を測る仕組み | `WP-SAFE-00` | 未着手 |
| 5 | Wi-Fi の改善・パスワード混入の是正 | `WP-NET-01` | 未着手 |
| 6 | **物理非常停止を効くようにする**（現在は無効化されている） | `WP-ESP32-01` | 未着手 |
| 7 | **止まるまでの距離を実測**（通電） | `WP-MEAS-01` | 未着手 |
| 8 | 人が歩いている部屋で地図を作れるか確認（通電） | `WP-MEAS-02` | 未着手 |
| 9 | 今の手押し作業の時間を測る（比較用・機体のみ） | `WP-MEAS-03` | 未着手 |
| 10 | 通信の途切れ具合を実測 | `WP-MEAS-04` | 未着手 |
| 11 | 1 日通しの完走試験 ※段階 6 の後に行う | `WP-MEAS-05` | 未着手 |

> **6 を 7・8 より先に行う。**物理非常停止が効かない機体で「急停止の距離」を測ることはできないため。

**この段階の終わり**: 測った値が台帳に入り、単体試験が通る。

### 段階 1 — 骨格（6 件・[詳細](detailed/DetailedDesign-wp1.md)）

| # | 作業 | 記号 | 状態 |
| --- | --- | --- | --- |
| 1 | 状態を管理する本体 | `WP-STATE-02` | 未着手 |
| 2 | 起動時の疎通確認（機器が全部繋がってから動き出す） | `WP-STATE-03` | 未着手 |
| 3 | 設定値の検査と読み込み（**危険な設定なら起動を止める**） | `WP-PARAM-02` | 未着手 |
| 4 | 画面の共通部分（ヘッダ・ボタン・警告窓） | `WP-UI-01` | 未着手 |
| 5 | 接続確認画面とメインメニュー | `WP-UI-02` | 未着手 |
| 6 | 手押しモード（非常停止を押すと手で押せる状態になる） | `WP-CARRY-01` | 未着手 |

**この段階の終わり**: 実機（電源断）で待機状態まで進み、**どの画面からでも非常停止が押せる**。

### 段階 2 — 安全装置（6 件・**最重要**・[詳細](detailed/DetailedDesign-wp2.md)）

| # | 作業 | 記号 | 状態 |
| --- | --- | --- | --- |
| 1 | 指令が途切れたら車輪を止める | `WP-SAFE-02` | 未着手 |
| 2 | **LiDAR の死角の校正**（今は設定が壊れている） | `WP-CALIB-01` | 未着手 |
| 3 | 異常の重さを 2 段階に分ける（軽い＝一時停止／重い＝非常停止） | `WP-SAFE-01` | 未着手 |
| 4 | **障害物リミッタ**（進行方向に物があれば速度を落とし、最後は止める） | `WP-SAFE-03` | 未着手 |
| 5 | 動いてはいけない場面で手動操作を無効にする | `WP-SAFE-04` | 未着手 |
| 6 | **13 通りの故障を意図的に起こす自動試験** | `WP-TEST-01` | 未着手 |

**この段階の終わり**: 意図的に壊しても止まることを、**人が見ていなくても**確認できる。

### 段階 3 — 手動で走らせる（4 件）

| 作業 | 記号 |
| --- | --- |
| 走行操作パネル（スティック・速度プリセット） | `WP-UI-03` |
| 手動走行 | `WP-TRANSIT-01` |
| 走った道の記録と保存 | `WP-ROUTE-01` |
| 教示（手動で走って道を覚えさせる） | `WP-TRANSIT-02` |

### 段階 4 — 人について行く（5 件）

| 作業 | 記号 |
| --- | --- |
| 人の検出と候補の一覧化 | `WP-PERC-01` |
| 追従走行 | `WP-TRANSIT-03` |
| 追従画面（候補レーダー・対象選択） | `WP-UI-04` |
| 教示（追従しながら道を覚えさせる） | `WP-TRANSIT-04` |
| 「追跡を切った」と「追跡が壊れた」の区別 | `WP-STATE-04` |

### 段階 5 — 覚えた道を走る（4 件）

| 作業 | 記号 |
| --- | --- |
| 地図の管理（試験場用と経路用を分ける） | `WP-ROUTE-02` |
| 自己位置を見失ったら止まる | `WP-SAFE-05` |
| 経路の再生（順走・逆走） | `WP-ROUTE-03` |
| 再生画面 | `WP-UI-05` |

### 段階 6 — 試験場での 1 日（6 件）

| 作業 | 記号 |
| --- | --- |
| 2 点を指して場所と向きを登録する | `WP-ONSITE-01` |
| 配電盤・待機場所への移動 | `WP-ONSITE-02` |
| 呼び寄せ時に人が退くのを待つ | `WP-ONSITE-03` |
| 地図の修正（人の映り込みを消す） | `WP-ONSITE-04` |
| 試験準備の画面 | `WP-UI-06` |
| 試験当日の画面 | `WP-UI-07` |

### 段階 7 — 点検と校正（6 件）

| 作業 | 記号 |
| --- | --- |
| 始業点検 4 項目 | `WP-MAINT-01` |
| 校正（直進・旋回・IMU）と履歴 | `WP-MAINT-02` |
| 校正結果を機体へ反映する | `WP-ESP32-02` |
| 故障診断 | `WP-MAINT-03` |
| 点検・校正・設定の画面 | `WP-UI-08` |
| **古いプログラムの削除** | `WP-CLEAN-01` |

### 段階 8 — 後回し

カメラの選定（`WP-LINE-00`）だけ**先に行う**（発注に時間がかかるため）。ライン誘導と電子リードの実装は未着手。

---

## 5. 各段階の確認方法

| 段階 | シミュレーション | 実機 |
| --- | --- | --- |
| 0 | — | **測定 4 件**（通電） |
| 1 | 起動して待機まで | 電源断で待機状態まで |
| 2 | **故障注入 13 項目** | 電源断で停止確認／**通電で物理非常停止** |
| 3 | 狭い部屋 | 通電で手動走行 |
| 4 | 散らかった部屋／見失う場面 | 通電で追従 |
| 5 | 広い場所 | 通電で教示 → 再生 |
| 6 | 配電盤の往復 | 通電で前日準備 ＋ 当日運用 |
| 7 | — | 通電で点検・校正 |

**自動試験は全段階で共通**（部品ごとの単体試験 → 結合試験。段階 2 以降はランダム入力試験を追加）。

```bash
bash scripts/run_tests.sh              # 単体試験のみ（最速）
bash scripts/run_tests.sh --all --sim  # 単体 ＋ 結合 ＋ シミュレーション
```

---

## 6. 次にやること

| # | 作業 | 理由 |
| --- | --- | --- |
| **1** | **実装環境を作る**（Ubuntu 22.04 ＋ ネイティブ Docker Engine ＋ ホスト側 Python） | **全パケットの完了条件が `docker compose run --rm th_robot` を前提にしている。**加えて `./data:/root/th_data` の bind mount 追加（無いと `--rm` で校正値・教示経路が毎回消える） |
| 2 | 段階 0 の作業単位をもう一度レビューする | これから実装する範囲。レビューは 1 回しか通していない |
| 3 | **部品と測定器を手配する**（状態 LED ＋抵抗、分圧抵抗、**テスタ**、**巻尺**） | 段階 0〜2 で要る。着手してから気づくと止まる。テスタは `WP-ESP32-01`（駆動電源の遮断確認）、巻尺は `WP-MEAS-01`（制動距離）で代替手段が無い |
| 4 | 段階 0 の 1〜3（型・状態遷移表・設定台帳）に着手 | 実機が不要なので、いつでも始められる |

> **`VISION.md` の更新は完了済み**（§1）。実装着手前の必須作業はこれで残っていない。

---

## 7. 用語

| 語 | 意味 |
| --- | --- |
| **作業単位（`WP-…`）** | 1 セッションで完結する作業のまとまり。48 個ある |
| **状態遷移表** | 「どのモードで・何を操作したら・何が起きるか」を並べた表。**表にない動きは起きない** |
| **故障注入** | わざと壊して、ちゃんと止まるかを確かめる試験 |
| **教示** | 人が一度走らせて、その道を機体に覚えさせること |
| **通電 / 電源断** | 通電＝モータを回せる状態。電源断＝モータの電源だけ切って PC と繋いだ状態 |
| **Gazebo** | ロボットのシミュレータ。実機を使わずに動作を確かめる |

---

## 8. 関連文書

| 文書 | 中身 |
| --- | --- |
| [`detailed/README.md`](detailed/README.md) | **詳細設計書。**各作業単位の実装内容と完了条件 |
| [`detailed/DetailedDesign-packets.md`](detailed/DetailedDesign-packets.md) | 48 作業単位の一覧・依存関係・順番の制約 |
| [`spec/README.md`](spec/README.md) | 完全設計書。何が・どう振る舞うべきか |
| `VISION.md`（リポジトリ直下） | 目指す完成形。**確定した方針はここが正** |
| [`detailed/DetailedDesign-open.md`](detailed/DetailedDesign-open.md) | 未解決事項とレビュー記録 |
