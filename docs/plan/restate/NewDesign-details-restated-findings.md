# 再表現による詳細ファイル 4 本の点検結果

`NewDesign.md` からリンクされている詳細ファイル 4 本を再表現した際に、
補完が必要だった箇所・食い違っていた箇所をまとめたもの。**この文書も正ではない。**

| 原文 | 再表現版 |
| --- | --- |
| `NewDesign-transit.md` | [NewDesign-transit-restated.md](NewDesign-transit-restated.md) |
| `NewDesign-onsite.md` | [NewDesign-onsite-restated.md](NewDesign-onsite-restated.md) |
| `NewDesign-operationalCheck.md` | [NewDesign-operationalCheck-restated.md](NewDesign-operationalCheck-restated.md) |
| `NewDesign-calib.md` | [NewDesign-calib-restated.md](NewDesign-calib-restated.md) |

分類は [NewDesign-restated-findings.md](NewDesign-restated-findings.md) と同じ
**(a)** 他に書いてある ／ **(b)** 他に違うことが書いてある ／ **(c)** どこにも無い。
`NewDesign-checklist.md` で既に追跡されているものは「既知」と記し、**未登録のものを中心に**挙げる。

---

## 1. 特に確認してほしい 6 件

読んで認識と合っているか、ここだけでも見てほしい順。

| # | ファイル | 内容 |
| --- | --- | --- |
| **D-1** | onsite | **当日手順 1〜10 は「順にやること」ではなく「2 つの行き方」である。** 2〜6 が (a) ピンを選ぶ、7〜10 が (b) 呼び寄せる。一本の番号付きリストなので、**ピンで盤へ行ったあと必ず呼び寄せもする**ように読める。再表現版 §4 では 2 つに割った |
| **D-2** | onsite | **「同じ位置のボタンを2回おして位置と向きを登録する」が二通りに読める。** ①**同じボタン**を違う 2 か所で押す（＝2 点指示。向きが出る）／②**同じ位置**でボタンを 2 回押す（＝1 点しかなく向きが出ない）。手順 6「少し手前に立って登録開始」から①のはずだが、この一文だけでは②とも読める |
| **D-3** | onsite | **待機場所の「姿勢」がどう決まるか書かれていない。** 手順 3〜4 は「待機場所に立って登録 → この待機場所、姿勢を地図初期位置とする」。だが**人の立ち位置からは向きが出ない**（だから盤の登録では 2 点指示・平面認識を用意している）。待機場所には同じ手当てが無い。当日の初期位置推定の基準がここなので効く |
| **D-4** | onsite | **呼び寄せ (b) で盤に対する向きをどう決めるのかが当日手順に無い。** (a) は「到着後超信地旋回で向きを合わせる」＝登録済みの向きがあるから合わせられる。(b) には登録済みの向きが無い。`onsite-recommendation.md` は (b) にも 2 点指示を使うとしているが、原文の当日手順には出てこない |
| **D-5** | transit | **追従走行のとき障害物が出たらどうするかが書かれていない。** 手動走行＝「警告する／自動ブレーキは**既定オフ**」、教示再生＝「停止する／自動回避しない」と方式ごとに書かれているのに、追従走行だけ記述が無い。`NewDesign.md` L132 は全方式共通の強制ルールに読める（→ 本体 findings N-1） |
| **D-7** | transit | **区画線走行のカメラと電子リード走行のリードデバイスが、`NewDesign.md` のハードウェア構成に載っていない。**（2026-08-15 追記分）構成表は Wi-Fi AP / PC / ESP32 / RaspberryPi4 / LiDAR / IMU / CuGoV3 の 7 項目のまま。この 2 方式を PoC で作るなら**機器が 2 つ増える** |
| **D-6** | transit | **教示のとき「対象が合っているかの確認」を飛ばす。** 通常の追従走行は手順 3〜4 で「確認」を押して機体を対象へ向け、左右に動いて追従を確かめてから走り出す。教示では**この確認を省略**し、代わりに走行開始時の向きを記録する。つまり**別人を掴んだまま記録が始まりうる**。意図した割り切りかどうか |

---

## 2. `NewDesign-transit.md`

| # | 分類 | 内容 |
| --- | --- | --- |
| T-r1 | **(c)** | **D-5**（追従走行の障害物挙動が無い）。★★★ |
| T-r2 | **(c)** | **D-6**（教示時に対象確認を省略する影響）。★★ |
| T-r3 | **(c)** | **教示の終わり方が無い。** 「方式を選んだ直後から記録開始」とあるが、**記録をどこで止めるか**（＝経路の終点をどう確定するか）が書かれていない。追従走行の手順 10〜11「停止させる／終了させる」がそれに当たると読めるが、明示がない。★★ |
| T-r4 | **(c)** | **「停止」と「終了」の違いが未定義。** 追従走行は手順 10「停止」・11「終了」と分けるが、手動走行は「終了」だけ。停止＝走るのをやめる／終了＝方式を抜ける、と解釈したが根拠は無い。★ |
| T-r5 | **(c)** | **「機体は試験員が近くにいる場合は止まる」のしきい値が無い。** また「試験員」が**追従対象**なのか**その辺にいる人**なのかも読めない。★★ |
| T-r6 | **(c)** | **「地図の更新をいつでもオンオフできるようにする」の対象が不明。** 教示再生走行中に走行しながら地図を書き足すのか、書き足した結果を保存するのかが読めない。★★ |
| T-r7 | **(c)** | **「既存経路の再設定」を選んだとき、元のデータがどうなるかが無い**（上書きか別保存か、途中で失敗したら元は残るか）。★ |
| T-r8 | **(b)** | 保存先が「**docker ワークスペース**」。`NewDesign.md` は「Ubuntu ネイティブに変更」（→ 本体 findings N-3）。★★ |
| T-r9 | **(b)** | **逆再生は独立方式ではなく教示再生の内部オプション**（手順 3）。`NewDesign.md` の KPI 表が独立方式として番号を振っているため書き漏れに見える（→ 本体 findings N-10）。★ |
| T-r10 | **解決** | 区画線走行・電子リード走行の手順が **2026-08-15 に原文へ追記された**（チェックリスト T-9 / R-4）。再表現版 §7・§8 は想像で書いたものを原文どおりに差し替え済み。**教示（手動走行）単独の手順は引き続き差分 1 行のみ。** |

### 区画線走行・電子リード走行の追記で新たに出た指摘（2026-08-15）

| # | 分類 | 内容 |
| --- | --- | --- |
| T-r11 | **(c)** | **カメラが `NewDesign.md` のハードウェア構成に無い。** 区画線走行は「カメラで区画線、区画線分岐を認識」する方式だが、§ハードウェア構成の 7 項目（Wi-Fi AP / PC / ESP32 / RaspberryPi4 / LiDAR / IMU / CuGoV3）にカメラが入っていない。**何台・どこ向き・どこに繋ぐか**が決まっていない。★★★ |
| T-r12 | **(c)** | **リードデバイスもハードウェア構成に無い。** 「RaspberryPi4 に USB 接続する Arduino のデバイス」と原文にあるが、構成表には出てこない。**RaspberryPi4 は LiDAR も USB で繋いでいる**ので、ポートと帯域の取り合いになる。ROS2 ノードをどちらが持つかも未定義。★★ |
| T-r13 | **(c)** | **区画線走行で線をロストしたときの挙動が無い。** `NewDesign.md` の KPI には「線のロスト率」「線が無い／途切れる区間の割合」があり、**起きる前提**で測ろうとしている。★★ |
| T-r14 | **(c)** | **区画線走行で障害物を検知したあとどうするかが無い。** 「LiDAR は障害物検知にのみ」と**用途だけ**書かれている。電子リード走行には障害物の記述自体が無い。→ T-r1（追従走行）と同じ穴が 2 つ増えた。★★★ |
| T-r15 | **(c)** | **電子リード走行の速度がどう決まるかが無い。** 「引っ張られること・引っ張られる向きで操る」とあるが、**張力の大きさで速度が変わるのか一定速か**が読めない。★★ |
| T-r16 | **(c)** | **電子リードの「離したら止まる」が明文化されていない。** 張力を見ることから導けるだけ（`NewVISION.md` 案 9-A はこれをこの方式の要点としている）。**意図して離したのか落としたのか**の区別も無い。★★ |
| T-r17 | **(c)** | **終わり方が方式ごとに違う。** 区画線＝**目的地で自動停止**、電子リード＝**人が「停止」を押す**、追従＝人が停止させる、教示再生＝到着したら停止。**区画線走行だけが自分で終点を判定する。** 統一するのかどうかが書かれていない（チェックリスト T-6「試験場に着いたと誰が判定するか」に直結）。★★ |
| T-r18 | 参考 | **区画線走行の初期姿勢の与え方が、盤登録の 2 点指示と同じ幾何である。** 「現在地をクリック」＋「向き方向の位置をクリック」＝方向ベクトル。1 点では向きが決まらないので 2 点目で与える、という同じ考え方が `NewDesign-onsite.md` と 2 か所で使われている。**共通化できる可能性がある**（UI としても実装としても）。★ |
| T-r19 | 参考 | **区画線走行のマップ編集は「[以降設計はあとにまわす]」と原文が明示している。** 書き漏れではなく意図的な後回しなので、チェックリストには「未設計と宣言済み」として載せるのが正しい。★ |

---

## 3. `NewDesign-onsite.md`

| # | 分類 | 内容 |
| --- | --- | --- |
| O-r1 | **(表現)** | **D-1**（当日手順が一本のリストだが実は 2 つの行き方）。★★★ |
| O-r2 | **(表現)** | **D-2**（「同じ位置のボタンを2回おして」が二通りに読める）。★★★ |
| O-r3 | **(c)** | **D-3**（待機場所の姿勢の決め方が無い）。★★★ |
| O-r4 | **(c)** | **D-4**（呼び寄せ時の向きの決め方が当日手順に無い）。★★★ |
| O-r5 | **(b)** | **2 点指示の押す順が `onsite-recommendation.md` §3.3 と逆。** 原文は「①少し手前 → ②立ってほしい位置」、あちらは「①立ってほしい位置 → ②盤へ一歩寄った点」。ベクトルの向きは同じになるが、**どちらの点がゴールになるか**が入れ替わる。どちらが正か決める必要がある。★★ |
| O-r6 | **(c)** | **平面認識 1 ボタン登録（手順 8）で、機体が「配電盤前」をどう知るのかが無い。** 「試験員が退いたあと配電盤前まで移動し認識する」とあるが、盤の位置はまだ登録されていない。試験員が立っていた位置へ行くのか、押した時点の位置からなのか。★★ |
| O-r7 | **(c)** | **当日の戻り先の選択肢が (a) と (b) で違う。** 手順 5（(a) のあと）は「待機場所**もしくは次の配電盤**」、手順 9（(b) のあと）は「待機場所」だけ。呼び寄せのあとは次の盤を選べないのか、単に書き分けていないだけか。★★ |
| O-r8 | **(c)** | **前日手順 11「1 ボタンで待機場所に戻す」時点で、地図はまだ保存されていない**（保存は手順 13）。作りかけの地図で自己位置推定して戻ることになる。想定どおりか。★ |
| O-r9 | **(b)** | **登録は「ロスト後短時間でも拒否」＝猶予なし。** `NewDesign.md` §人物追跡の「500 ms 以内のロストは停止しない」は登録には効かない（→ 本体 findings N-4）。★★ |
| O-r10 | **(b)** | 保存先が「docker ワークスペース内」（→ 本体 findings N-3）。★★ |
| O-r11 | 既知 | 「試験員が退き次第」の判定方法（D-6）、作業中ボタンの正式インターフェース（P-1）、到着ずれのリカバリ（D-4）、ピンの編集・削除（M-6）は未定義のまま。★★★〜★★ |

---

## 4. `NewDesign-operationalCheck.md`

| # | 分類 | 内容 |
| --- | --- | --- |
| C-r1 | **(表現)** | **対象は 4 項目なのに「理想的な機能（3 項目共通）」「3 項目すべての結果を一覧に集約」のまま。** 緊急停止ボタンを足したときの直し漏れ。★ |
| C-r2 | **(表現)** | **走行を伴うのは「モーター・エンコーダ」だけ**なのに、その安全設計（押している間だけ動く／`IDLE`・`MANUAL` 以外では始められない）が**4 項目共通**の欄に書かれている。全項目で機体が動くように読める。★ |
| C-r3 | **(c)** | **緊急停止ボタンの確認手順が 1 行だけ。** 押す／離すの両方を試すのか、画面がどう状態を出すのか、NG のときどこへ行くのかが無い。他の 3 項目は流れが書かれている。★★ |
| C-r4 | **(c)** | **総合ステータスが NG のとき運用に入れるのかが無い。** 「このまま使い始めてよいかを一目で判断できる」とあるが、**止めるのか警告だけか**は書かれていない。`NewDesign.md` §起動の「必須機器が繋がらない場合」と同じ穴。★★ |
| C-r5 | **(c)** | **いつこの確認をやるのかが無い。** `NewDesign.md` §導入では初期導入時に 1 回。毎日の起動時にもやるのかは書かれていない。★ |
| C-r6 | **(c)** | **モーター・エンコーダが NG だったときの行き先が無い。** IMU・LiDAR には較正への導線があるが、**符号のズレや断線は較正では直らない**ため受け皿が要る。★★ |

---

## 5. `NewDesign-calib.md`

| # | 分類 | 内容 |
| --- | --- | --- |
| K-r1 | **(c)** | **「許容範囲」の数値が 4 項目とも無い。** 共通の性質に「同じ動作をもう一度行い、**誤差が許容範囲に収まったこと**を確認してから完了」とあるが、その許容範囲が決まっていないと**検証ステップの合否が出せない**。★★ |
| K-r2 | **(c)** | **「要再較正」の判定基準が無い。** 一覧画面に「要再較正（前回 07/20）」「ドリフト増加を検知」と出す設計だが、何をもってそう判定するかが書かれていない。★★ |
| K-r3 | **(c)** | **補正値の保存先が無い。** 教示データ・地図は保存先が明記されているのに、較正の補正値と履歴をどこに置くかが書かれていない。「永続化」「履歴とロールバック」を求めているので置き場所が要る。★★ |
| K-r4 | **(表現)** | **共通ウィザードの骨格（案内→実行→実測入力→プレビュー→検証）が全項目には当てはまらない。** 当てはまるのは 1 直進・2 旋回。3 IMU は実測入力もプレビューも無く自動完了、4 死角は実測入力の代わりに画面でなぞる。「他の項目も概ね同じ骨格」と書かれているが、実際は 2 項目。★ |
| K-r5 | **(c)** | **較正中に E-Stop / フォルトで止まったあと、どう再開するかが無い。** 「即座に停止」までは決まっている。やり直しか続きからか。★ |
| K-r6 | **(c)** | **死角マスクの編集・削除が無い。** 「追加」しか書かれていない。★ |
| K-r7 | 既知（原文が自認） | **直進較正だけコンパイル時定数**で、即時反映できない。原文自身が「是正すべきギャップとして扱う」と明記しているが、**是正の方法は書かれていない**。★★ |

---

## 6. ファイルをまたぐ指摘

| # | 内容 |
| --- | --- |
| Z-1 | **較正画面にモード制限の記述が無い。** 動作確認は「`IDLE` / `MANUAL` 以外では確認自体を始められない」と明記している。較正はそれより危険（**自律で一定距離・角度を動く**）なのに、同じ制限が書かれていない。★★ |
| Z-2 | **動作確認の 4 項目と較正の 4 項目が対応していない。** 動作確認＝緊急停止／モーター・エンコーダ／IMU／LiDAR、較正＝直進／旋回／IMU／死角マスク。**IMU と LiDAR は 1 対 1 で繋がるが、モーター・エンコーダの受け皿が較正側に無い**（直進・旋回は「較正」であって「故障の検知」ではない）。→ C-r6 と同じ穴。★★ |
| Z-3 | **「docker ワークスペース」が transit / onsite の両方に残っている。** `NewDesign.md` は Ubuntu ネイティブへ変更。直すなら 2 か所同時（→ 本体 findings N-3）。★★ |
| Z-4 | **ロスト時の扱いが用途ごとに違うのに、本体には 1 つの規則しか書かれていない。** 登録＝猶予なし（onsite）、追従・呼び寄せ＝500 ms 猶予（本体）。本体側に「用途ごとに違う」と書かないと読み違える（→ 本体 findings N-4）。★★ |

---

## 7. 原文 → 再表現版 の対応（抜けの確認用）

| 原文の節 | 再表現版 |
| --- | --- |
| **transit**: 冒頭 / 追従走行 / 追従走行 使用方法 / 追従走行 教示 / 手動走行 / 手動走行 使用方法 / 教示再生走行 / 教示 / 教示再生走行 使用方法 / 地図更新 / **区画線走行** / **区画線 事前準備** / **区画線 使用方法** / **電子リード走行** / **リードデバイス** / **電子リード 使用方法** | §0 / §2 / §2 進行 / §4 / §3 / §3 進行 / §5 / §4 / §5 進行 / §5 地図の更新 / §7 / §7.1 / §7.2 / §8 / §8.1 / §8.2 |
| **onsite**: 前日 13 手順 / 当日 10 手順 / 登録について / Nav2 について / 配電盤前 | §2＋§3 / §4 / §3 受付条件 / §5 / §6 |
| **operationalCheck**: 対象 / 理想的な機能 6 点 / 項目別 4 節 / 画面イメージ | §1 / §3＋§5 / §4 / §6 |
| **calib**: 対象 / 理想的な機能 8 点 / 項目別 4 節 / 現状ギャップ / 画面イメージ | §1 / §3＋§2 / §4 / §5 / §6 |

原文の全節が再表現版のどこかに入っている。逆に、**再表現版にあって原文に無い節はもう無い**
（2026-08-15 の追記で、想像で書いていた区画線走行・電子リード走行が原文どおりの内容に置き換わった）。
