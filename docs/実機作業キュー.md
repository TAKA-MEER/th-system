# 実機作業キュー

**PC だけでは進められない作業を、段階をまたいで 1 箇所に溜める。**機体が使える日に
ここを上から見て計画を立てる。`docs/機体作業手順-段階0.md` が「その日の手順書」で、
こちらは「何が待っているか」の一覧。**PC 作業中に実機待ちが増えたら必ずここに足す。**

最終更新: 2026-09-06（教示再生走行試験 第1〜4回・ESP32 シリアル化の実機導入を反映）

> **【2026-09-06 現在の状況】** この文書の大半は 2026-08-30 時点の記述で、以降の実機作業が
> 反映されていない箇所がある。**先に済んでいること**:
>
> | 項目 | 状態 |
> | --- | --- |
> | ESP32 ファームの再書き込み（§0-2） | **済**。2026-09-05 にシリアル版ファームを書き込み（ラズパイの esptool 経由） |
> | ESP32 の受信ギャップ（§4） | **解消**。WiFi をやめてラズパイ直結のシリアルにしたため、原因 A（TCP の HOL blocking）ごと消滅（§4 参照） |
> | 走行を伴う 3 項目のブロック（§1.2.1） | 2026-09-01 に解除済み（本文どおり） |
> | 教示再生の実走行 | 3階コース 4 回・短距離 2 回を実施。結果は [教示再生走行試験.md](教示再生走行試験.md) |
>
> **いま実機で最優先の作業**: ラズパイの `rplidar.service` を `/dev/ttyUSB0` 直指定から
> `/dev/serial/by-id/...` へ直すこと（[setup.md](setup.md) §6-1）。ESP32 が同じラズパイに
> 挿さった今、列挙順が入れ替わると `/scan` が死ぬ。次回試験の事前条件でもある
> （[試験項目.md](試験項目.md) §2）。

---

## 0. 最優先 — 焼く前の関門

| # | 作業 | 状態 |
| --- | --- | --- |
| **0-1** | `config.h` の `#define ESTOP_BENCH_TEST_BYPASS` をコメントアウト | **済**（2026-08-31・`a36d42c`）。ソース上はバイパス無効。**焼くまでは機体上のファームは依然バイパス有効**なので走らせない |
| **0-2** | **ESP32 ファームの再書き込み** | **済**（2026-09-05）。シリアル版ファーム（`serial_link`）を書き込み済み。**書き込み方法が変わった**（ESP32 はラズパイに USB 接続されているので、ラズパイ上の esptool で焼く。§0.1） |
| **0-3** | 焼いた直後に `/safety/firmware_flags` を確認 | `0` なら正常（バイパス無効）。`0xFF` は「旧ファームのまま＝不明＝バイパスの可能性あり」。`1` はバイパスが有効なので**その場で焼き直す** |

> 0-2 のあと、`ros2 topic echo /safety/firmware_flags` と物理ボタンの押下・解放
> （ラッチしないこと）を確認する。手順は `機体作業手順-段階0.md` §9.1。

### 0.1 PlatformIO はこの PC で使える（コンパイル検証済み）

**「PlatformIO がこの PC に無くコンパイルは未検証」という以前の記述は誤りだった。**
`~/.platformio/penv/bin/pio` にあり、ESP32 ツールチェーンも
`th_ws/esp32/.pio/` の過去のビルド成果も揃っている。2026-08-31 に**焼く前に**
両方コンパイルして確認した（`ws_link.cpp` の `sendEstopHw` 3 byte 化（`e210286`）が
未コンパイルだったという不確定要素はこれで解消）。

| 対象 | 結果 |
| --- | --- |
| バイパス有効のまま（変更前） | SUCCESS / Flash 905701 bytes |
| バイパス無効（`a36d42c`） | SUCCESS / Flash 905713 bytes |

```bash
cd th_ws/esp32
~/.platformio/penv/bin/pio run -e esp32dev              # ビルドのみ
~/.platformio/penv/bin/pio run -e esp32dev -t upload    # 焼く（PC に直結しているとき）
~/.platformio/penv/bin/pio device monitor -b 115200     # シリアルログ
```

> **【2026-09-05 更新】焼き方が変わった。** ESP32 は**常時ラズパイに USB 接続**
> されるようになった（通信もこのシリアル経由。無線は使わない）。PC からは
> `-t upload` できないので、**PC でビルドして `.bin` をラズパイへ送り、ラズパイ上の
> esptool で焼く**（手順は [esp32.md](esp32.md)。書き込み中は
> `sudo systemctl stop rpi-serial-relay` でポートを空けること）。
> PC に直結して焼く場合だけ上のコマンドが使える。
>
> なお `pio` はこの PC に 2 つある（`~/.platformio/penv/bin/pio` と、
> 2026-09-05 に `pip3 install --user` で入れた `~/.local/bin/pio`）。どちらでもビルドできるが、
> **`pip` 版を入れると依存の `anyio` が pytest プラグインとして登録され、ホストの
> `python3 -m pytest` が全滅する**（`-p no:anyio` で回避。[testing.md](testing.md)）。
>
> 上表の「`ws_link.cpp` の `sendEstopHw` 3 byte 化が未コンパイル」という当時の懸案は、
> **`ws_link.cpp` ごと削除された**（`serial_link.cpp` へ置換）ため消滅した。

---

## 1. 実機がないと完了と言えない残作業

**段階 2 を起動できなくする `registry.yaml` のブロッカーは 0 件になった（2026-08-30）。**
`blind_angle_ranges` は「走行体のみの構成では LiDAR に死角が無い」ことを確認した結果を
`status: measured` / `value: []` として記録し、`blocking` を外した（`88c2ab1`）。
死角 0 個と「未校正」の区別は配列の形ではなく `blind_calibrated`（class:b・`given: true`）が担う
（空配列は `sanitize_node_params()` が落とすため、配列からは両者を区別できない）。

つまり以下はもう「段階を進めるためのブロッカー」ではなく、**PC 側は実装済みで、
実機で確認しないと完了と言えない残作業**である。

| WP | 段階 | 実機で何をするか | PC 側の到達点 |
| --- | --- | --- | --- |
| ~~`WP-CARRY-01`~~ | 1 | ~~④ モータードライバ電源が実際に落ちることをテスタで確認~~ | **完了（2026-08-31）。§1.3 に実測値。§10 ①〜④ すべて到達** |
| `WP-SAFE-03` | 2 | **障害物へ向けて実際に走らせて止まること**（距離判定の妥当性。通電） | 実装完了。sim で `/cmd_vel` の publisher が 1 個・20.002 Hz を確認済み。ブランチ `feat/wp-safe-03-obstacle-limiter`（**未マージ**。不合格ならブランチ上で調整して本線を汚さない） |
| `WP-TEST-01` | 2 | **故障注入 04 / 08 / 10**（人が張り付く。§7 参照） | 14 本を ctest 登録。内訳は下の「故障注入の現状」。ブランチ `feat/wp-test-01-fault-injection`（**未マージ**） |
| `WP-CALIB-01` | 2 | **上部構造を積んでから**死角を実測する。走行体のみの現構成では実機作業なし（通電不要） | `01a`（`scan_geometry` の Python/C++ 等価性 21,045 件）完了。`01b`（`calib_runner` / `/calib/*` / S-40）**未着手** |
| `WP-SAFE-04` | 2 | `WP-SAFE-03` の実機確認が通ってから着手（`jog_gate` 自体は通電不要） | 未着手 |
| `WP-MEAS-02` | 0 | 人が歩いている部屋で地図を作れるか | 通電 |
| `WP-MEAS-03` | 0 | 手押し搬送のベースライン計測 | 機体のみ（通電不要） |
| `WP-MEAS-05` | 0 | 1 日通しの完走試験 | 段階 6 の後 |

### 1.1 故障注入の現状（`WP-TEST-01`・14 本）

2026-08-30 に故障注入 12 を実装し、**未実装は 0 件になった**。Docker 実測の内訳:

| 結果 | 件数 | 内訳 |
| --- | --- | --- |
| pass | 6 | 01 障害物前進 ／ 03 UI 非常停止 ／ 05 通信断→フォルト ／ 11 リミッタ SIGKILL ／ **12 `/cmd_vel` 途絶** ／ control 対照 |
| skip | 2 | 07（段階 6 で有効化）／ 13（段階 5 で有効化）。設計書 §11 の指示どおり |
| **明示的失敗（実機専用・T-3）** | 3 | **04 ／ 08 ／ 10** — 手順は §1.2。`pytest.skip` にせず赤で居続けるのが正しい状態 |
| 明示的失敗（前提未達） | 2 | 02（`v_reverse` が未解決。`blind_clearance_m` 待ち。§2 の注参照）／ 09（`PersonTargets` の publisher がリポジトリに 1 つも無く、`SAFETY_ENABLED_TARGETS` に person を足すだけでは解決しない） |
| 明示的失敗（未解決） | 1 | 06（`N-25`。100 ms の予算を満たせない。**安全挙動そのものは 05 と 11 で担保されている**——満たせないのは時間の上限だけ） |

段階 2 の出口チェックは「実機専用 3 本を除く 10 本が通る」ことしか要求していないので、
04 / 08 / 10 が赤いままでも出口判定は妨げない。

**対照ケース（`fault_injection_control`）を消さないこと。**これが無いと「常に止まっている
壊れた実装」でも 01〜13 が全部通る。01 と control が両方通って初めて意味を持つ
（設計書には無い追加だが必須）。

### 1.2.1 走行を伴う 3 項目のブロックと、その解除（2026-08-31 判明 → 2026-09-01 解除）

> **解除済み（2026-09-01）。**S-11 手動走行画面を実装した（`WP-TRANSIT-01`）。
> 以下は「なぜ実施できなかったか」の記録として残す。次の実機日は
> `feat/wp-transit-01-manual` をチェックアウトして §7 の 6・7・8 を実施できる。
> **ただし実機で画面遷移まで確かめたわけではない**（PC 上の e2e までしか通していない）。
> 最初に `/system/state.speed_limit` が `v_max` になることをトピックで確かめること。

**故障注入 04・`WP-SAFE-03`・故障注入 08 は、機体ではなく WebUI 側の未実装でブロックされていた。**
実機作業キューはこれらを「実機さえあれば実施できる」と書いていたが、誤りだった。

**機構**（実機で経路を 1 段ずつ追って確定した）:

1. `obstacle_limiter` が `/cmd_vel_muxed=0.100` を `/cmd_vel=0.000` にクランプする。
   `/safety/limiter_status` は `action=STOP` / `applied_limit_mps=0.000` /
   `nearest_obstacle_m=1.863`——**障害物は無関係**で、上限そのものがゼロ。
2. 上限の出所は `/system/state.speed_limit = stop`。
3. `zones.py` の `SCREEN_SPEED_LIMIT` は **`S-00: stop` / `S-01: stop`**。
   走行できるのは `S-10`〜`S-16`（`v_max`）・`S-20/21`（`v_slow`）等の**走行画面に居るときだけ**。
4. ところが `web_ui/src/screens/` には **`S00Connect.jsx` と `S01Main.jsx` しか無い**。
   `main.jsx` が描画するのも `AppShell` ＋ この 2 画面だけ。
   ジョグ操作（`/cmd_vel_manual` を押している間だけ流す）を持つのは**旧実装の `App.jsx`** で、
   新シェルから到達できない。

**つまり「メインメニューから手動走行画面へ移動できない」のは不具合ではなく、その画面が無い。**
設計上、走行画面に居ない限り速度上限は `stop` から外れない
（`derive_limits()` は使用中の画面が 0 台のときも `stop` にフェイルセーフする。`Spec-safety.md` §6.2）。

**併せて判明した別件**: 旧実装 `App.jsx` のジョグは `mode === MODE.MANUAL` で有効化されるが、
この `mode` は **`useRosbridge()` が購読する `/robot/mode`（廃止予定の旧 FSM）**由来である。
新 FSM（`/system/state`）を `MANUAL` にしても UI 側は反応しない。実機では
`/mode_manager/set_mode` を別途叩いて旧 FSM も `MANUAL` にする必要があった
（故障注入 11 で前任者が踏んだ「廃止予定の `/robot/mode` を見ている」のと同じ型）。

**解除の条件だったもの**: 走行画面の実装。実機を必要としない PC 作業だった。

**2026-09-01 に実施した 3 パケット**（いずれも変異テストで検出力を確認済み）:

| WP | 内容 | ブランチ |
| --- | --- | --- |
| `WP-SAFE-04` | `jog_gate` 新設。`/cmd_vel_manual_raw` → `jog_gate` → `/cmd_vel_manual` | `feat/wp-safe-04-jog-gate` |
| `WP-UI-03` | 走行タブ（`VirtualStick` / `SpeedPreset` / W-6） | `feat/wp-ui-03-drive-tab` |
| `WP-TRANSIT-01` | **S-11 手動走行画面。**S-01 から到達できる | `feat/wp-transit-01-manual` |

**なお当初「S-10 系の画面を 1 枚作る」と書いていたのは誤り。**手動走行は **S-11**
（S-10 は追従走行で `WP-UI-04`）。さらに画面 1 枚では足りず、`jog_gate` と
走行タブを含む 3 パケットが要った（[wp3](../docs/plan/detailed/DetailedDesign-wp3.md) を参照）。

---

### 1.3 `WP-CARRY-01` の実機確認結果（2026-08-31・完了）

`§10 ③`（電源断での遷移）と `§10 ④`（通電でのテスタ確認）を実施した。

**③ 遷移**（モータ電源断・`stage:=1` / `lidar_source:=network`）:

| 事象 | 観測 |
| --- | --- |
| 押下 | `MANUAL → CARRY`、`estop_hw=True`、`prev_mode=MANUAL` をラッチ |
| `CARRY` 中の `ui.estop.press` | **拒否**（`accepted=False` / `reject_reason_key='estop_disabled_in_carry'`） |
| 解放 | `estop_hw=False`。**`CARRY` のまま**（明示的な resume を要求する設計どおり） |
| `ui.carry_resume` | `MANUAL/PAUSE` へ復帰（`prev_mode` から正しく戻る） |

**④ テスタ実測**（通電）。手順は「先にボタンを押してから通電する」順にした
（駆動が通電した状態でボタンを押す順にしないため）:

| 状態 | モータードライバ電源ライン |
| --- | --- |
| **物理ボタン押下中** | **0.5 V** |
| **解放後** | **26.52 V** |

**合格**。押下でハード的に駆動電源が落ちている。残留 0.5 V は開放接点の漏れ・
テスタの負荷・ブリーダ経由の残留と考えられ、26.52 V に対し 1.9 %。
モータードライバが動作する電圧ではない。**この 2 値は次回の比較基準にすること**
（値が上がっていたら接点の劣化を疑う）。

**副次的に確認できたこと**: 一連の操作でフォルトは 0 件。モータードライバの
通電・遮断そのものは `safety_monitor` の監視対象を一切揺らさない。

> **細かいが記録しておく点**: ボタン解放の直後に `last_reject_reason='not_allowed'`
> が 1 回立つ（`CARRY` 中の `hw.estop.release` に対応する遷移が無いため）。
> 動作上は無害だが、この値は `/system/state` に載るので **UI がそのまま出すと
> 「操作が拒否されました」と誤って見せうる**。UI 側の扱いを決めるときに考慮すること。

---

### 1.2 実機専用 3 本（04 / 08 / 10）の実施手順

`case_04` / `case_08` / `case_10` の docstring は「個別の実施手順はこのファイルに
無いので、実施時に手順と結果をここへ追記すること」と書いている。以下がその手順である。
**実施したら結果（日付・実測値・所見）をこの節に追記すること。**
3 本とも `pytest.fail` のまま赤で居続けるのが正しい状態なので（T-3）、
「実施済み」の記録はこの文書にしか残らない。

#### 故障注入 04「非常停止（物理）」

> **前提**: `ESTOP_BENCH_TEST_BYPASS` を外したファームが焼けていること（§0）。
> **バイパスが有効なままだと押しても何も起きず、しかもテストは「押しても止まらない」
> という正しい観測を返す。**先に §0-3 で `/safety/firmware_flags` が `0` であることを確認する。

| 順 | 操作 |
| --- | --- |
| 1 | **先に記録を始める。**別端末で `ros2 topic echo /safety/estop_hw` と `ros2 topic echo /cmd_vel`、`ros2 topic echo /system/state` を回しておく（押してから echo を始めると遷移の瞬間を取り逃す） |
| 2 | `MANUAL` ジョグで低速前進させる |
| 3 | 走行中に物理ボタンを押す |

**合格条件**: `/safety/estop_hw` が `true` になり、`/cmd_vel` がゼロになり、車輪が止まる。
`/system/state` の `mode` が `ESTOP` になる。**押下 → 停止の実測値を記録すること**（合否だけでは次に活かせない）。

#### 故障注入 08「ESP32 ウォッチドッグ」

> **落とすのは `esp32_bridge` を含む ROS2 側全体。**`obstacle_limiter` だけを落としても
> `esp32_bridge` が生きていればキープアライブが 20Hz でゼロを送り続けるため、
> **ESP32 のウォッチドッグは発火しない**（それは故障注入 12 の担当範囲であり、
> この項目の観測対象ではない）。ここで見たいのは「PC が丸ごと死んでもファームだけで止まるか」。

| 順 | 操作 |
| --- | --- |
| 1 | 車輪が空転しても安全な状態にする（機体を持ち上げるか、十分な空間を確保する） |
| 2 | 低速前進させる |
| 3 | 走行中に PC 側の ROS2 を丸ごと落とす（`launch` の `Ctrl-C` より `kill -9` の方が「PC の突然死」に近い） |

**合格条件**: `esp32_watchdog_ms`（600 ms・`config.h` の `WATCHDOG_MS`）以内に車輪が止まる。
ESP32 のシリアルログ（`[DBG]` 行）で停止時刻を裏取りできるとなおよい。

> **後始末**: `kill -9` を繰り返すとコンテナ内の DDS discovery が壊れる
> （`CLAUDE.md` の「環境の癖」）。この項目の後は `ls /dev/shm | wc -l` を確認し、
> 必要ならコンテナを再起動してから次へ進む。

#### 故障注入 10「物理ボタン起動時押下」

| 順 | 操作 |
| --- | --- |
| 1 | 物理ボタンを押した（またはラッチさせた）状態にする |
| 2 | その状態でシステムを起動する |
| 3 | `INIT` から出ないことを確認する |
| 4 | **ボタンを解放し、`IDLE` へ進むことを確認する** |

**合格条件**: 手順 3 で `/system/state` の `mode` が `INIT` のまま（`connectivity_checker` の
L-2 が `/safety/estop_hw` = true の間 `link_ok` を出さない）、かつ UI に解除の案内が出る。
**そのうえで手順 4 が通ること。**

> **手順 4 を省かないこと。**`connectivity_checker` は `/safety/estop_hw` を
> **一度も受信していない間も**「判定できない＝不合格」として `INIT` に留める
> （`_estop_seen`。§6.2）。つまり「起動しなかった」だけでは、
> **ボタン押下を検出して止めたのか、そもそも通信が来ていないだけなのかを区別できない。**
> 解放したら進むところまで見て初めて「押下を検出していた」ことが示せる。


#### 故障注入 10 の実施結果（2026-08-31・**部分合格**）

`stage:=1` / `lidar_source:=network`、物理ボタンを押したまま起動した。

| 時刻 | 観測 |
| --- | --- |
| 起動〜123.7s | **`INIT/CHECK` のまま変化 0 回**（`estop_hw=True`）。`evt.link_ok` は 1 度も出ない |
| +123.7s | 解放（`estop_hw=False`） |
| **+124.3s** | **`INIT → IDLE`**（解放から **0.6 秒**） |

**「運用に入れない」は合格。**解放したら 0.6 秒で `IDLE` へ進んだので、
`INIT` に留まっていたのは**ボタン押下を検出していたから**であって、
通信が無かったからではないことが示せた（§1.2 の「手順 4 を省かないこと」の狙いどおり）。

**「解除を案内する」は未達。**合格条件の後半が満たされていない。
`state_manager` は案内の効果を正しく発火させているが、**宛先が無いので捨てられている**:

```
state_manager: effect 'guide' args={'key': 'estop_held_at_boot'}
  -> 宛先 'WebUI' は未実装のため 配送せず捨てる（WP-STATE-02 §11 既知の負債）
```

つまり **FSM 側は完成していて、UI への配送だけが繋がっていない。**
`WP-STATE-02` §11 の既知負債を解消すれば、`case_10` 側の変更なしに合格条件を満たす。
**この項目を「合格」と書かないこと** — 押したまま起動した運用者には現状何も表示されない。

---

## 2. 未測定パラメータ（`registry.yaml` の `status: placeholder`）

2026-08-30 時点で 16 件（`blind_angle_ranges` が `measured` になり 1 件減った）。
**段階 2 をブロックするものは 1 件も残っていない**（最も早いもので段階 4 から）。
**機体が要るもの**:

| パラメータ | blocking | 何を測るか |
| --- | --- | --- |
| `tracker_lost_grace_ms` | 段階 4 から | 人物ロストの猶予。`person_timeout_ms` の導出元 |
| `person_position_sigma_m` | 段階 4 から | 人物位置の分散 |
| `replay_drift_m_per_100m` | 段階 5 から | 教示再生の走行ドリフト |
| `calib_interval_days` ／ `calib_linear_tolerance_ratio` ／ `calib_rotation_tolerance_deg` ／ `calib_blind_tolerance_deg` | 段階 7 から | 校正の許容幅 |
| `leash_stop_latency_ms` | 段階 8 から | リード停止の遅れ |
| `battery_endurance_min` | 段階 8 から | バッテリ持続時間（通電） |
| `ui_gap_p99_ms` | 段階 8 から | **機体＋タブレット。**下記 N-5 |

**会場で測るもの**（機体は不要・巻尺）: `venue_clearance_m` ／ `blind_clearance_m` ／
`panel_clearance_m` ／ `corridor_width_m`

> **`blind_clearance_m` は巻尺だけでは埋まらない。**`v_reverse`（後退の速度上限）の逆算元だが、
> `note` のとおり `O-a2`（盤の数・間隔・盤前の空間）が決まるまで placeholder のままである。
> これが埋まらないと `v_reverse` が解決できず、**故障注入 02（障害物リミッタ・後退）が
> 前提未達で実行できない**。走行体のみの現構成では LiDAR に死角が無いので「死角起因の
> クリアランスは 0」と置ける可能性があるが、それは測定ではなく**設計判断**なので
> 人が決める必要がある（`DetailedDesign-open.md` `O-a2`）。

**人が決めるもの**（測定ではない）: `restart_max_count` ／ `restart_wait_ms`（`O-d4`）

---

## 3. 未解決事項のうち実機が要るもの

`DetailedDesign-open.md` §4.1 に登録済み。

| # | 内容 | 実機で何をするか |
| --- | --- | --- |
| **N-5** | UI 側の受信ギャップが 30 秒窓 p99 で 4592 ms（`meas04_after.csv`）。タブレットのスリープかブラウザのタイマー抑制の混入が疑われる | タブレットのスリープを切った状態で再測定し、切り分ける。`ui_gap_p99_ms` を埋める |
| **N-6** | `esp32_timeout_ms = 322 ms` に対し `esp32_watchdog_ms = 600 ms`。**ESP32 が自分で止まる前に PC 側が「切断」と判定する** | どちらを動かすか決めた上で、実機で誤発動の頻度を確認する |

---

## 4. ESP32 の受信ギャップ（**解消済み・2026-09-05**）

> **【解消】原因 A（TCP の HOL blocking）ごと構成から取り除いた。**
> 第4回3階教示再生走行試験（2026-09-05）で通信エラーにより教示できなかったことを受け、
> **ESP32 の無線を廃止し、ラズパイへ USB-UART で直結**する構成に変更した
> （[network.md](network.md) / [教示再生走行試験.md](教示再生走行試験.md) §5）。
> ESP32↔ラズパイ間は TCP を通らないので、この節が挙げる原因 A は原理的に発生しない。
> 導入後の実測では `/esp32/wheel_feedback` は約100ms間隔・標準偏差 0.003〜0.01s。
>
> 残る原因 B（ファーム側の詰まり）・C（`esp32_bridge` の GIL 競合）は、
> 現在この症状として観測されていない。**再発したときのために以下の切り分け記録を残す。**
>
> なお導入直後に**似た症状が一度再発した**が、原因は新設の `pi_serial_relay` 側で、
> `ser.read` の固定50msポーリングが ESP32 の100ms送信周期とビートを起こしていたもの
> （`ad3960d` で解消）。**同じ「毎周期バースト」の見え方をするので混同しないこと。**

以下は無線構成だった当時（〜2026-09-05）の記録。

2026-08-21 の実測（`th_ws/data/meas04_after.csv`・22 分）で、**esp32 軸に 600 ms 超の断が
3 回、最悪 4106 ms** 残っている。p99（161 ms）は健全だが裾が健全でない。
`esp32_timeout_ms = 322 ms` なので、**実機では誤フォルトではなく「本物の断」による停止が
起きる。**

`meas05` の切り分け（`th_ws/data/meas05/*_verdict.txt`）による原因は 3 つ:

| 原因 | 内容 | 直せる場所 |
| --- | --- | --- |
| **A** | TCP の HOL blocking（WebSocket は TCP。1 フレーム落ちると再送で 200〜600 ms 途切れる） | UDP 化か WS フレーム分割の見直し。**設計判断が要る** |
| **B** | ESP32 が送っていない（IMU の I2C 読みか制御ループの詰まり） | **ファーム。実機必須** |
| **C** | `esp32_bridge`（Python）の詰まり（50 Hz の `_drain_rx_queue` と GIL 競合） | **PC のみで直せる。**実機を待たなくてよい |

無線区間そのものは健全（ICMP 3000 発が全て 33 ms 以内・損失 0 %。
`meas04/ping_icmp_600s.md`）。**AP を替えても直らない。**

### 4.1 切り分けは 1 回の走行で済むようになった（2026-08-26）

`esp32_bridge` に**自己計測の計器を入れた**（本線 `fdb72bd`）。50 Hz の drain タイマーが
実際に何 ms 遅れて呼ばれたかを測り、1 Hz で機械可読のサマリを出す。

```
esp32_bridge_diag: timer_interval_max_ms=..., drain_duration_max_ms=..., feedback_gap_max_ms=...,
frames_since_window=..., frames_total=..., coalescer_dropped_total=..., rx_queue_dropped_total=...,
invalid_frames_total=...
```

**判定の仕方**（`ros2 launch` のログを `grep esp32_bridge_diag` で拾うだけ）:

| 観測 | 結論 |
| --- | --- |
| `feedback_gap_max_ms` が跳ねた窓で `timer_interval_max_ms` **< 50** | **C は否定。**原因は A か B。`meas05_esp32_triage.sh` の tcpdump で A/B を分ける |
| 同じ窓で `timer_interval_max_ms` **が跳ねている** | **C が確定。**bridge 側が実際に止まっている |
| `rx_queue_dropped_total` / `coalescer_dropped_total` が増える | 輻輳が実在する。値は捨てた件数そのもの |

**tcpdump より先にこれを見ること。**ログを見るだけで 3 択が 2 択以下に減る。

### 4.2 実機計測で原因 C は否定された（2026-08-31）

**この計器は設計どおりに効いた。**実機 bringup を 190 秒動かすだけで 3 択が 2 択に減った。

| 指標 | 実測（190 窓） |
| --- | --- |
| `feedback_gap_max_ms` 中央値 | 100.8 ms（10 Hz の周期どおり） |
| 同 p90 | 120.1 ms |
| 同 **最大** | **1640.8 ms** |
| `esp32_timeout_ms`(322ms) 超の窓 | **3 回**（1240.2 / 1400.3 / 1640.8 ms） |
| `timer_interval_max_ms` | 20.9（**< 50**） |
| `rx_queue_dropped_total` / `invalid_frames_total` | **0 / 0** |

**判定: 原因 C（`esp32_bridge` の詰まり）は否定。**残るは A（TCP の HOL blocking）か
B（ESP32 が送っていない）。

### 4.3 A/B の切り分け結果（2026-08-31）: **A（TCP）。ただし発生条件が分かった**

`meas05_esp32_triage.sh` を条件を変えて 2 回（各 600 秒）回した。**差分は `/scan` を
購読するかどうかだけ**（ラズパイ AP 構成では LiDAR も ESP32 も同じ無線区間を通るため、
`/scan` を受け続けること自体が電波の占有を再現する）。

| 条件 | ESP32 (ROS) 最大 | 電線上 最大 | TCP 再送 | 250ms 超 |
| --- | --- | --- | --- | --- |
| `/scan` 購読**なし** | 未取得※ | **131.1 ms** | **0 / 18,616** | **0 回** |
| `/scan` 購読**あり** | **1220.9 ms** | **927.2 ms** | **8 / 18,601** | **1 回** |

※ 1 回目は `meas05_arrival_recorder.py` が欠損していて起動せず、ROS 側が取れなかった。
これ自体は事故だったが、結果として「`/scan` を受けない条件」の対照実験になった。

```
t+528.1s  ROS 1220.9ms  電線上 927.2ms  再送 8 件  → A（TCP 再送）
```

**結論: A（伝送方式）。AP を替えても直らない。**ただし今回分かったのは判定だけではない:

1. **跳ねは「LiDAR と ESP32 が同じ無線区間を共有しているとき」にだけ出た。**
   共有しない条件では電線上のギャップ 0 回・再送 0 件で、まったく再現しない。
2. **LiDAR 自身は無傷**（ROS 側 max 179.0 ms・250ms 超 0 回）。
   競合したとき壊れるのは **ESP32 側だけ**という非対称がある。TCP で小さなフレームを
   高頻度に送る ESP32 が、再送とバックオフの影響を受けやすいのだと考えられる。
3. **ROS 側（1220.9ms）が電線上（927.2ms）より約 294 ms 大きい。**
   支配項は電線側だが、`esp32_bridge` 側にも上乗せがある。

> **2026-08-20 の判定（B: ESP32 が送っていない）と食い違う。**当時は ESP32 が AP の構成で、
> 跳ねも 1 回・最大 300 ms だった。今日は 1 回・電線上 927 ms で再送 8 件という直接証拠がある。
> **どちらも跳ね 1 回に対する判定なので断定はしない**が、構成が変われば支配的な原因も
> 変わりうるということ。**構成を変えたら必ず測り直すこと。**

**対処の候補**（未決。設計判断が要る）:

| 案 | 効果 | 代償 |
| --- | --- | --- |
| ESP32 リンクを UDP 化 | 再送とバックオフが消える。根本対処 | ファームと `esp32_bridge` の両方を書き換える。順序保証を自前で持つ |
| WS のフレーム分割・送信頻度の見直し | 占有時間を減らす | 効果は限定的かもしれない |
| **LiDAR と ESP32 を別の無線に分ける**（帯・チャネル・物理 I/F） | 競合そのものを無くす。今回の実測が直接支持する | 機材構成の変更 |
| LiDAR を有線化（ラズパイ〜PC を Ethernet） | 無線区間から `/scan` が消える | 配線の取り回し |

**`esp32_timeout_ms = 322 ms` に対し実測 1220.9 ms なので、この跳ねは実運用で
「本物の断」として停止を起こす**（N-6）。対処が入るまでは、走行試験中に
`ESP32_DISCONNECTED` が出ても機材の故障ではなくこれを疑うこと。

**頻度は meas04 より悪化している。**meas04 は 22 分で 600 ms 超が 3 回だったが、
今回は約 3 分で 322 ms 超が 3 回（うち 3 回とも 1.2 秒以上）。ネットワーク構成が
当時と違う（PC は USB WiFi ドングル経由で `th-rpi-ap` に接続）ため、
**ドングル側の可能性も切り分け対象に加えること。**

> **ping を WiFi 品質の指標に使わないこと。**同日の実測で ESP32 への ICMP は
> avg 85.8 / max 150.7 ms と悪い値を示したが、同じリンク上の実データは 10.00 Hz・
> std dev 0.4 ms で極めて安定していた（AP 自身への ICMP は 1.4〜3.1 ms）。
> ESP32 の省電力モードが ICMP 応答だけを遅らせている。`esp32_bridge_diag` を見ること。

なお C の**発生機構自体は本線で塞いである**（送信のコアレッシング・受信キューの有界化・
ログの throttle。`bedc984` / `bcc80d9` / `97337ca` / `c516a53` / `544617e`）。
それでもなお C が出るなら、塞いだ経路以外に原因があるということになる。

---

## 5. 段階 1 の出口確認（通電不要・モータ電源断）

段階 1 の完了条件は「実機（**モータ電源断**）で `INIT → IDLE → MANUAL` が確認でき、
どの画面からでも非常停止が押せる」こと。`WP-UI-01` / `WP-UI-02` / `WP-CARRY-01` の
実装が終わってから、機体に PC を繋いで確認する。**車輪は回さない。**

---

## 6. 記録先の不整合（解消済み）

`機体作業手順-段階0.md` §7 が完了条件として検査するパスに `docs/plan/detailed/data/`
配下を挙げていたが、このディレクトリは統合ブランチに存在せず、実測データは
`th_ws/data/` にしかないというズレがあった。2026-08-26 に解消済み。

**決定した方針: 記録先は `th_ws/data/` に一本化する。**
`docker-compose.yml` がコンテナへバインドマウントするのは `src` / `esp32` /
`scripts` / `data` / `dr_spaam_weights` のみで、`docs/` はマウントされていない。
計測スクリプトはコンテナ内で走るため `docs/` 配下には直接書けず、「書ける場所」と
「記録する場所」を一致させる必要があった。以後、新しい計測データも `docs/` 配下ではなく
`th_ws/data/` に置くこと。

対応内容:

| 項目 | 対応 |
| --- | --- |
| `WP-MEAS-01`（制動） | `th_ws/data/brake_accel_*.csv` として既に存在。`registry.yaml` の `source` を実パスに修正 |
| `WP-MEAS-02` | 未実施のまま。`th_ws/data/meas02/` へ記録する（手順書 §7 参照） |
| `WP-MEAS-03` | 未実施のまま。`th_ws/data/meas03/baseline.md` へ記録する（手順書 §7 参照） |
| `WP-MEAS-04` | `recovered/feat/meas-latency-triage` から改善前の比較用生データ 12 ファイルを `th_ws/data/meas04/` へ取り込み、`th_ws/data/meas04_after.csv` を `th_ws/data/meas04/after.csv` にリネームして揃えた |
| 寸法（`dimensions/body.md`） | **恒久的に消失。**値は `registry.yaml`（`body_width_m` / `body_length_m`）の `note` に出典注記付きで生存しており再計測不要 |

手順書 §7 と `registry.yaml` の `source` / `note` は上記に合わせて更新済み（値は変更していない）。

---

## 7. 実機日の進め方（推奨順）

上から順に。**前の項目が通らないうちに次へ進まない**（特に 1 → 2）。

| 順 | 内容 | 通電 | 参照 |
| --- | --- | --- | --- |
| 1 | `ESTOP_BENCH_TEST_BYPASS` をコメントアウト → ビルド → 焼く | 不要 | §0-1 / §0-2 |
| 2 | `/safety/firmware_flags` が `0`、物理ボタンの押下・解放が検出されること | 通電 | §0-3 |
| 3 | 段階 1 の出口確認（`INIT → IDLE → MANUAL`・どの画面からも非常停止）。**車輪は回さない** | モータ電源断 | §5 |
| ~~4~~ | ~~`WP-CARRY-01` ④ モータードライバ電源のテスタ確認~~ | 通電 | **完了 2026-08-31**（§1.3） |
| 5 | 故障注入 **10**（物理ボタン押下起動）— 走行を伴わないのでここで済ませる | 通電 | §1.2 |
| 6 | 故障注入 **04**（物理非常停止・走行中） | 通電・走行 | §1.2 |
| 7 | `WP-SAFE-03` 障害物へ向けて走らせて止まること | 通電・走行 | §1 |
| 8 | 故障注入 **08**（ESP32 ウォッチドッグ）— DDS を壊す可能性があるので**走行を伴う項目の最後**に置く | 通電・走行 | §1.2 |
| 9 | 余力があれば `WP-MEAS-02` / N-5 / N-6 / `grep esp32_bridge_diag` | 通電 | §3 / §4.1 |

### 7.1 チェックアウトするブランチに注意

**作業線は 1 本のスタックである。**

> **【2026-09-06 更新】いま使うのは `feat/demo-teach-replay`。**
> 段階 0〜3 の成果は PR #20 で `main` へ統合済みで、以降は特例デモ運用
> （手動教示・教示再生）としてこのブランチで進めている。ESP32 のシリアル化
> （`feat/esp32-serial-via-pi`）も PR #21 でこのブランチへマージ済み。
> 下表は 2026-09-01 時点の記録として残す。

| ブランチ | 扱い |
| --- | --- |
| **`feat/demo-teach-replay`** | **これを使う（2026-09-06 現在）。**教示再生デモ＋ESP32 シリアル化を含む |
| ~~`feat/wp-transit-01-manual`~~ | 2026-09-01 時点の先端。段階 0〜3 は `main` へ統合済み |
| `feat/wp-ui-03-drive-tab` / `docs/wp3-detailed-design` / `feat/wp-safe-04-jog-gate` | 上の祖先。段階 3 の途中段階 |
| `feat/wp-test-01-fault-injection` | 段階 0〜2 までの作業線。`safe-03 ← calib-01a` を包含する |
| `feat/wp-safe-03-obstacle-limiter` / `feat/wp-calib-01a-scan-geometry` | 上の祖先。単独でチェックアウトする理由は無い |
| `feat/stage0-1-integration` | **使わない（古い）。**`lidar_filter.py` が `scan_geometry` 委譲前の旧版 |
| `recovered/*`（10 本） | 復旧用のスナップショット。内容は作業線に取り込み済み |
| `main` | 段階 0〜2 が 1 件も入っていない（作業線が 155 commits 先行） |

> **2026-09-01 の訂正**: 以前ここには「本線＝`feat/stage0-1-integration`」と書いてあったが誤り。
> 2026-08-31 の実機日の修正 6 件を `stage0-1-integration` と `wp-test-01-fault-injection` の
> **両方に別々にコミットした**ため、スタックの土台（`stage0-1-integration ⊂ calib-01a`）が折れ、
> 2 本が別々に進む状態になっていた。内容は作業線側が新しいので、`stage0-1-integration` に
> しか無かったもの（`CLAUDE.md` の環境の癖 2 件）を作業線へ移して整流した。

実機で不合格が出た場合はこの作業線の上で直す。
