# 試験項目（今回）

[← README に戻る](../README.md) ／ 動かし方は [使い方.md](使い方.md)

**この文書は試験のたびに書き換える。** 動かし方は書かない（[使い方.md](使い方.md) が正）。
ここには「今回何を確かめるか」「どこまで行けたら合格か」「何を記録するか」だけを書く。

| | |
| --- | --- |
| 試験名 | **校舎1周の教示再生（長距離化）** |
| 起票日 | 2026-09-02 |
| ブランチ | `feat/demo-teach-replay` |
| 実施日 | （記入する） |
| 実施者 | （記入する） |

---

## 1. この試験のねらい

10m 以内の教示再生は成功している。**100m 以上（校舎1周）でも成立するか**を確かめる。

長距離で新しく効いてくるのは次の 3 つ。今回はここを見る。

1. **SLAM が長時間・広範囲で安定して動くか**（これまで最大 14m × 7m の地図しか作っていない）
2. **教示した経路が地図と重なって見えるか**（ループ閉じ込みは切ってあるので、
   地図に継ぎ目ができても経路と地図がずれずに追従が成立するか）
3. **終点の誤差がどれくらいか**（これが 1 経路の長さの上限を決める）

---

## 2. 事前条件

- [ ] 床に**始点マーク**（ビニールテープの十字）を貼り、機体の向きも合わせられる状態にした
- [ ] 走らせる1周のルートを下見した（扉が閉まっていないか、段差・カーペットの継ぎ目が無いか）
- [ ] 走行中に人が付き添える（いつでも止められる位置に居る）
- [ ] `enable_route_slam:=true` を付けて起動した（[使い方.md](使い方.md) §1-2）
- [ ] 健全性確認 5 項目がすべて通った（[使い方.md](使い方.md) §2）

---

## 3. 試験項目

### T-1. 起動の安定性

| | |
| --- | --- |
| やること | `enable_route_slam:=true` で起動し、5 分間放置する |
| 見るもの | `[FAULT]` が出るか。「非常停止中」の窓が出るか。CPU に余裕があるか |
| **合格** | 起動から 2 分以内にすべて `[FAULT CLEARED]` になり、以後 3 分間フォルトが出ない |
| 記録 | 「非常停止中」が出た回数: ____ 回 |

CPU の確認（別ターミナル・コンテナ内）:

```bash
docker exec th_robot ps -eo pcpu,args --sort=-pcpu | head -8
```

> 合計が常時 100% を大きく超えているようなら記録して報告する。

---

### T-2. 教示で1周する

| | |
| --- | --- |
| やること | 始点マークから出発し、校舎を1周して始点マークに戻る（[使い方.md](使い方.md) §3-3） |
| 速度 | **低速（0.15 m/s）または中速（0.30 m/s）**。速いと記録が粗くなる |
| **合格** | 途中で止まらず1周でき、「保存しました」が出る |
| 記録 | 記録距離: ____ m ／ 点数: ____ 点 ／ 所要時間: ____ 分 ／ 経路名: ________ |

走行中に**地図が伸び続けている**ことを画面で確認し続ける。伸びが止まったら
その場所を記録する（長い直線廊下で起きやすい）。

- [ ] 地図が最後まで伸び続けた
- [ ] 伸びが止まった場所があった → 場所: ________________

---

### T-3. 教示した経路が地図と重なって見えるか（壁に埋まっていないか）

| | |
| --- | --- |
| やること | 教示直後の地図を見る（[使い方.md](使い方.md) §4-2） |
| **合格** | **経路（点列）の線が地図の壁と重なって表示されている**（壁の中に埋まっていない） |
| 記録 | 経路と地図が合っている ／ ずれている |

開始地点付近の壁が**二重に見えても正常**（ループ閉じ込みを切っているため。詳細は
[使い方.md](使い方.md) §4-2）。ここで見るのは「**経路が地図と同じフレームに載って
いるか**」のほう。

- [ ] **地図と経路のスクリーンショットを撮る**（教示直後）

経路が地図の壁に埋まっている・大きくずれている場合: [使い方.md](使い方.md) §4-3 の
リセットを実施してから T-4 へ進む。

```bash
docker exec -it th_robot bash /root/th_ws/scripts/reset_map.sh
```

最後に「完了」と出ることを確認する。**実施したかを記録する。**

- [ ] リセットを実施した ／ 実施していない

---

### T-4. 再生で1周する

| | |
| --- | --- |
| やること | ロボットを始点マークに戻し、順再生で1周させる（[使い方.md](使い方.md) §3-4） |
| **合格** | 経路を外れずに完走し、「一時停止（記録の終端に達しました）」が出る |
| 記録 | 完走した ／ 途中で止まった（場所: ____________） |

走行中に見るもの:

- [ ] 教示した通り道からの**横ずれ**が目視でどれくらいか（最大: 約 ____ cm、場所: ________）
- [ ] 障害物での自動停止が働いたか（____ 回）
- [ ] 「非常停止中」が出たか（____ 回）
- [ ] **地図のスクリーンショットを撮る**（再生直後）

---

### T-5. 終点の誤差を測る ★この試験の主目的

| | |
| --- | --- |
| やること | 停止した位置で、機体の基準点（前面中央）から**始点マークまでの距離**をメジャーで測る。向きのずれも測る |
| **合格の目安** | 位置 **50cm 以内**・向き **10 度以内**（初回なので参考値。実測することが目的） |
| 記録 | 位置のずれ: ____ cm ／ 向きのずれ: ____ 度 |

この値から「1 経路として許容できる長さ」を決める。

---

### T-6.（余力があれば）逆再生

**画面の「逆再生」ボタンは現在ボタンが押せない状態になっている**（デモ範囲外として
未配線）。ノード側の逆再生は実装済みなので、試すならコンテナ内から直接指示する。

やり方: 「教示再生」画面で経路を選ぶところまでは同じ。**「この経路で進む」は押さず**、
代わりにコンテナ内で次を実行する（`<経路名>` は T-2 で付けた名前）。

```bash
docker exec -it th_robot bash
source /root/th_ws/install/setup.bash
ros2 service call /system/trigger th_system_msgs/srv/UiTrigger \
  "{trigger: 'ui.route_select', arg_json: '{\"id\": \"<経路名>\", \"reverse\": true}', requester: 'cli'}"
```

`accepted: true` が返れば受理。あとは画面が「推定できました」に変わるのを待って
**「再生」**を押す。

| | |
| --- | --- |
| **合格** | 完走する |
| 記録 | 受理された ／ されなかった（`reject_reason_key`: ________） |
| 記録 | 完走した ／ 途中で止まった（場所: ____________） |

> うまくいかなければ**飛ばしてよい**。この試験の主目的は T-5。

---

## 4. 中止の条件

次のどれかに当たったら**その場で試験を止めて報告する**。無理に続けない。

- ロボットが人・壁に向かって進み、「停止」で止まらない
- 「非常停止中」が短時間に 3 回以上繰り返し出る
- 教示中に地図が壊れる（壁が四方八方に散る）
- ESP32 との通信が途切れ続ける（`ESP32_DISCONNECTED` が消えない）

---

## 5. 提出するもの

- [ ] 本書（記入済み）
- [ ] 地図のスクリーンショット 2 枚（教示直後・再生直後）
- [ ] 経路ファイル `th_ws/data/routes/<経路名>.json`
- [ ] 起動ターミナルのログ（`[FAULT]` の回数が分かるもの）

---

## 6. 過去の試験

| 日付 | 内容 | 結果 |
| --- | --- | --- |
| 2026-09-01 | 10m 以内の教示再生 | 成功。ただし SLAM 無しで実施していたことが後日判明 |
| 2026-09-02 | `enable_route_slam:=true` の初回起動確認 | `/map`（284×147）と `map→odom` の生成を確認。走行は未実施 |
| 2026-09-03 | **校舎1周（本番）** | 教示は成功（**185.0 m / 1504 点 / 包絡 44.5×56.4 m**）。ループは閉じたが、その瞬間に記録点が **4.09 m 跳び**（index 1474→1475）、経路が地図とずれて壁に埋まった。再生は**別セッションの経路**（`start_yaw` −178.1°）を選んでしまい、逆向きに旋回して壁へ。1 周の経路は**保存されず** `.wip` だけ残った（自動保存が 185 m を救った）。→ 対策は WS-9K |
