"""
case_04_estop_physical.py — 故障注入 4「非常停止（物理）」（ctest 登録名: fault_injection_04）
=================================================================================
`DetailedDesign-safety.md` §10 #4: 走行中に物理非常停止ボタンを押し、停止することを確認する。
自動化: **実機のみ**（通電が必要。`Spec-safety.md` §9／同設計書 §10 表）。

【T-3: `pytest.skip` ではなく明示的に `pytest.fail` で落とす】
`pytest.skip` は結果が「緑寄り」（少なくとも失敗としては数えられない）になり、
このボタンが実機で本当に確認されたのか・されていないのかが CI/ctest の結果から
読み取れなくなる。このテストは自動化できないこと自体が仕様
（`DetailedDesign-wp2.md` `WP-TEST-01` §1「実機専用3本の自動化は作らない」）であり、
**恒常的に失敗し続けるのが正しい状態**。

段階2の出口チェック（`DetailedDesign-wp2.md` 「段階2の出口チェック」節）も
「実機専用3本を除く10本が通る」ことしか要求していないため、この
`fault_injection_04` が赤いままでも出口判定を妨げない。

【「実機で実施済み」を宣言する環境変数などの逃げ道を作らなかった理由】
一度「実施済み」を宣言できる変数を作ると、シェル環境や CI 設定に居座り続け、
「本当は未確認なのに緑」という、このテストがまさに防ごうとしている事故を
別の形で再現しかねない。実施記録は環境変数ではなく
`docs/実機作業キュー.md` および実機作業ログに残す運用とする
（上記の通り段階2の出口条件もこのテストの合否に依存していないため、
逃げ道を用意する実益もない）。
"""
import pytest

_PROCEDURE_NOTE = (
    "実機専用（通電必要）。手順書: docs/実機作業キュー.md §1 の `WP-TEST-01` 行"
    "（『故障注入13項目の自動化』『一部が通電必要』）を参照。"
    "この項目（#4 非常停止（物理））単体の実施手順はまだ同ファイルに個別記載が無いため、"
    "実施時に手順と結果をそこへ追記すること。"
    "仕様: docs/plan/detailed/DetailedDesign-safety.md §10 の表 #4 行。"
)


def test_fault_injection_04_estop_physical():
    pytest.fail(
        "故障注入4『非常停止（物理）』は実機のみで確認する項目であり自動化しない。"
        + _PROCEDURE_NOTE
    )
